# 开发进度

最后更新：2026-07-31

## 当前状态

- 消除 5 万图缩略图缓存写入的 O(N²) 热点：不再为每个新缩略图扫描整个平铺缓存
  目录并删除同一行的旧签名文件；缓存键已包含来源路径、文件长度、修改时间与尺寸层级，
  旧文件不会被命中，批量“更新现有图片”也不再逐行全目录清缓存，统一交给
  512 MiB 容量淘汰处理。全目录容量检查由“首次写入及
  每 32 次写入”改为“首次写入及每新增 64 MiB”，并取消可重建缓存逐文件
  `sync_all`，保留临时文件关闭后原子改名；缓存命中时直接从已读取的内存字节解析
  尺寸，不再为尺寸查询二次打开文件。Rust 全量库测试 240 项通过、1 项性能基准忽略，
  Clippy 严格检查、前端检查和生产构建通过（保留 3 条既有图片拖动可访问性警告）。
- 优化 5 万图规模下的资料库迁移：预扫描一次生成目录/文件清单，复制文件时用
  1 MiB 复用缓冲区同步计算 SHA-256，校验阶段只顺序读取目标副本，不再重新遍历
  来源树并交替读取源/目标文件；已经完整校验的暂存库和目标库不再重复执行打开时
  的内容哈希/元数据指纹回填。可重建的 `cache/thumbnails` 保留空目录但不复制，
  避免大量小文件拖慢迁移；SQLite 数据库仍使用 Online Backup 后执行完整性校验，
  Tag、分组、备注、提示词和设置不会被重置；回归测试新增 Tag 迁移保留断言。
- 工具箱“迁移资料库”新增真实分阶段进度：后端依次上报统计数据、复制
  非数据库文件、SQLite Online Backup、文件逐字节校验、数据库完整性校验和切换
  目录；复制/文件校验按字节计数，数据库备份按页数计数，界面同时显示当前阶段、
  已处理容量与单调总体进度条。进度事件限速为最多约每 100 ms 一条，避免大库
  复制时堵塞 WebView。新增六阶段顺序、进度单调和 100% 收尾回归测试；迁移测试
  4 项通过，Rust 生产库 Clippy 严格检查通过，前端检查 0 错误（保留 3 条既有警告）。
- 发布版本更新为 0.12.2（GitHub Release `v_28`）：修复 v_27 大库升级首启仍然
  "未响应"的问题。根因是回填每扫一张图就跨 IPC 发一条进度事件，数万行的事件
  以远高于渲染节奏的速率灌进 WebView 主线程，窗口白屏假死；同时结果一次性攒
  在内存、扫完才写库，中途退出全部进度丢失、下次从头重扫。现在进度事件按
  总量稀释为全程约 200 条，扫描结果每 500 行落库一批：界面全程流畅，中断后
  已写入的行不再重扫，内存占用为常数。新增 750 行分批与稀释回归测试；Rust
  全量库测试 236 项通过、Clippy 严格通过、前端检查 0 错误、生产构建通过。
- 发布版本更新为 0.12.1（GitHub Release `v_27`）：VIBE 索引回填移出打开数据目录
  的同步路径，改为前端启动后调用 `backfill_vibe_statuses` 命令在阻塞线程后台
  执行；右下角进度条实时显示"正在建立 VIBE 聚合索引 N / M"并注明是升级后
  一次性工作、期间可继续正常使用。完成且确实有补齐时刷新数据视图并通知结果
  （含不可读原图数量）；失败不阻塞使用，下次启动自动续跑；回填进行中关窗会
  被关闭守卫拦截确认。换库（打开其它数据目录）后同样自动触发。此前 v_26 的
  回填在窗口显示后同步执行，库大时界面假死数十秒且无任何提示。验证：Rust
  全量库测试 235 项通过（回填测试改为验证进度回调与结果结构），Clippy 严格
  检查通过，前端检查 0 错误，生产构建通过；Playwright 模拟 Tauri IPC 冒烟
  6 项全部通过（回填中界面可用、进度文案、切视图、完成通知、进度条消失、
  命令只触发一次）；debug 构建实测启动即响应、关闭正常。
- 发布版本更新为 0.12.0（GitHub Release `v_26`）：重复项视图新增“按 VIBE”
  聚合，数据库升级到 schema v16；修复关闭按钮因缺少 `allow-destroy` 权限
  完全失效的问题。发布前基线：Rust 库测试 235 项通过、1 项性能基准忽略，
  Clippy 严格检查通过，前端检查 0 错误（保留 3 条既有警告），生产构建通过。
- 修复主窗口与工具箱窗口关闭按钮完全失效的问题：UX 阶段1 引入关闭守卫
  （`onCloseRequested`）后，Tauri 2 规定放行关闭时由前端 API 调用
  `destroy()` 真正关窗，但两个窗口的 capability 都缺少
  `core:window:allow-destroy` 权限，导致关闭请求被静默拒绝——关闭按钮、
  系统关闭消息和确认框的“仍要关闭”全部无效。已在 `default.json` 与
  `toolbox.json` 补上该权限；用 debug 构建实测 WM_CLOSE 走守卫流程后
  进程正常退出。
- 重复项视图新增“按 VIBE”聚合（分支 `agent/minimal-ui`）：把引用了同一组
  NovelAI VIBE 的图片自动聚为一组。导入与更新现有图片时解析
  `reference_image_multiple`，对每项引用取 SHA-256、排序后再整体哈希，得到
  与引用顺序无关的组合签名；数据库升级至 schema v16（`rows.vibe_signature`
  列 + 部分索引，`dedupe_aliases` 放开 `vibes` 模式），v8–v15 自动升级，
  历史库首次打开时只为有 VIBE 但缺签名的行回填（文件不可读时写空串标记，
  不反复扫描）。重复项视图聚合依据新增第三项“按 VIBE”，组名默认显示
  “VIBE 组合 <前8位>”短标识，右键可设别名；成员分页、别名、Tag 筛选、
  排序与其它聚合模式共用同一管线。验证：Rust 全量库测试 235 项通过（新增
  签名顺序无关性、聚合/别名/成员、两类回填共 4 项）、Clippy 严格检查通过、
  前端检查 0 错误（保留 3 条既有警告）、生产构建通过；Playwright 模拟
  Tauri IPC 冒烟 8 项全部通过（入口按钮、聚合列表、短标识标题、成员展开、
  重命名别名、别名后不显示原始哈希、模式往返切换）。
- 发布版本更新为 0.11.0（GitHub Release `v_25`）：汇总 `v_24` 之后的自动规则、
  批量 Tag、画师前缀、界面重构、拖放搜图与交互稳定性改进；发布前基线验证为
  Rust 库测试 232 项通过、1 项性能基准忽略，Clippy 严格检查通过，前端检查
  0 错误（保留既有 3 条图片拖动可访问性警告）。Windows x64 NSIS Release
  安装器构建通过，大小 5,257,114 字节，SHA-256 为
  `2B86F0EDA4FBAA06EC7A7AC2B0D2AD0503B0DFA9C2EC27955E677179ABB9817B`。
- 自动规则新增“粘贴 JSON 文本”导入入口：可直接粘贴纯 JSON，或包含在一段 AI 回复中
  的单个 `json` 代码块；先在弹窗内检查，再复用文件导入的规则预览、重名改名、缺失
  Tag/分组提示、原子写入和强制停用流程，并可返回修改且不丢失原文。空内容、超过
  2 MB、错误/未闭合/多个/非 JSON 代码块均会拦截，预览和确认之间继续使用 SHA-256
  校验。AI 编写助手提示词同步说明可直接复制代码块并粘贴导入。新增 2 项 Rust 边界
  测试并通过，Clippy 严格检查通过；前端检查 0 错误（保留既有 3 条警告），生产构建
  通过。Playwright 在 1180px 完成错误提示、代码块预览、返回修改、确认导入和默认
  停用全流程，并在 900px 验证入口、弹窗和页面均无横向溢出；控制台仅有既有 favicon
  404。
- 根据实际 AI 对话测试补强自动规则提示词的领域背景：开头直接定义智能表格是专门
  存储和整理 AI 绘画图片的本地资料库，不是普通电子表格或通用文件管理器；明确当前
  主要面向 NovelAI 图片及其生成元数据，并列出提示词、模型、采样器、种子、生成数值、
  VIBE 和尺寸等字段；新增“图片提示词项”和“资料库 Tag”的强制区分，并明确图片
  提示词采用 `tag1,tag2` 形式的逗号分隔字符串，规则中的 `value` 不能误写为数组或
  自然语言描述。提示词检查、追加和精确删除三个章节均重复注明该约束。34 个 JSON
  片段重新通过解析检查，提示词无加粗标记，生产构建通过。
- 自动规则已嵌入 AI 编写助手提示词：侧栏新增“一键复制 AI 编写提示词”，点击时重新
  读取资料库并把最新 Tag/分组以安全 JSON 数组注入已审查的 Markdown 模板；特殊名称
  中的标签边界字符会转义，宏只对原模板做单次替换。模板包含需求多轮澄清、生成前
  最终确认、新名称逐项授权、完整条件/任务字段字典、AND/OR/整体取反语义、正则与
  空值边界、任务顺序和批量改写防呆；最终只输出一份保持停用的可导入 JSON。同步把
  “导入 JSON”由向外箭头改为向下进入容器的导入图标。文档内 34 个 JSON 片段均通过
  解析检查，并覆盖当前 14 类条件和 12 类任务。前端检查 0 错误、生产构建通过；
  Playwright 在 1180px 与 900px 下验证入口布局、图标路径、复制成功通知、最新列表
  二次读取、宏完整替换及特殊名称转义，控制台仅有既有 favicon 404。
- 自动规则新增 JSON 导入/导出：规则列表可导出当前已保存规则或全部规则，也可选择
  `.json` 文件导入。文件带专用格式标识和版本号，分组引用使用名称而非库内 ID；
  导入前弹窗预览规则、触发时机、条件/任务数量、重名改名和缺失依赖，确认后在单个
  数据库事务内创建必要 Tag/分组及规则。导入规则统一停用；文件限制 2 MB、200 条规则
  和 1000 个依赖，预览后内容被替换会由 SHA-256 校验阻止导入，错误 JSON、错误版本、
  外部数据库 ID 和无效规则均给出明确错误。新增跨库名称转换、重名、默认停用及坏文件
  回归测试；Rust 全量测试 230 项通过、1 项性能基准忽略，Clippy 严格检查通过，前端
  检查 0 错误且生产构建通过。Playwright 完成导入预览/确认、导入结果、当前/全部导出
  和 900px 窄窗口验证。
- 精简自动规则第 3 步的新增任务入口：移除“添加任务”按钮前重复的任务类型下拉框，
  点击按钮直接新增默认任务，再在任务卡片内统一选择类型，减少一次无意义的预先选择。
  前端检查 0 错误，生产构建通过。
- 自动规则第 3 步增加目标选择防呆：添加/移除 Tag 改为可搜索、多选的现有 Tag
  列表，显示关联图片数并支持刷新，不再允许手输或顺手创建新 Tag；移入分组必须
  显式选择现有分组，不再默认选中第一项。旧规则中已不存在的 Tag/分组不会静默丢失，
  而是保留原值并显示警告，方便用户安全更正。前端检查 0 错误、生产构建通过；
  Playwright 已覆盖搜索、多选、取消、空 Tag 库、失效旧值、显式选择分组及 900px 窗口。
- 详情面板的备注、正向提示词、角色提示词和负向提示词编辑框新增键盘保存：
  Enter 直接保存，Shift+Enter 保留换行，Esc 继续用于取消；中文输入法确认候选词
  时不会误触保存。保存成功后先同步行缓存再切换展示态，并取消退出编辑时的交叉
  淡入淡出，避免旧文本回显或界面闪烁。前端检查 0 错误。
- 画廊预览新增 Tag 摘要胶囊：主画廊卡片在图片左下角最多并排
  显示两个独立 Tag 胶囊，不再使用“首个 Tag +N”的数量摘要；更多 Tag 直接
  隐藏并保留在悬停提示中。胶囊按 Tag 库稳定顺序在 `#3C66BF`、`#5F9E56`、
  `#D1A54C`、`#E07EAD`、`#C36537`、`#714BCA` 六色之间循环，并为每种底色
  配置高对比文字。左侧 Tag 列表在名称前显示对应色块；选择框、VIBE 和 Tag
  分别固定在图片左上、右上和左下，互不重叠。
  分组与重复项共用的小卡片同步显示相同摘要，
  无 Tag 图片不显示胶囊，可直接通过卡片外观判断是否完成打标。前端检查
  0 错误且生产构建通过。
- 工具箱“以图搜图”新增参考图拖放导入：激活该工具时可把单张 PNG、JPG、BMP、
  GIF、WebP 或 TIFF 图片拖入顶部卡片，进入拖放区时显示明确高亮与“松开搜索”反馈，
  松开后复用原有感知哈希搜索流程；多文件或不支持格式会就地提示。工具切换后监听器
  不会误接收其他面板的拖放。前端检查 0 错误。
- 新增“筛选无 Tag 的图片”开关：主画廊、表格、重复项与分组视图的
  未分组区域统一传递该筛选；启用时与具体 Tag 筛选互斥，避免产生
  必然为空的条件组合。筛选条件同步覆盖顶部标签、空状态文案、全选当前
  结果、批量 Tag/分组/删除与各类导出。单张无 Tag 图片在详情面板新增
  Tag 后会立即移出结果集。后端使用 `row_tags` 主键索引的 `NOT EXISTS`
  谓词，查询缓存键也已纳入该条件。新增无 Tag 筛选及 Tag 变更后缓存
  刷新的回归测试；Rust 全量库测试 235 项通过、1 项性能基准忽略，
  Clippy 严格检查通过，前端检查 0 错误且生产构建通过。
- 多图打 Tag 重做 + 自动规则列表重设计（分支 `agent/minimal-ui`）：
  - 砍掉侧栏"打标"模式，侧栏收编为纯筛选（sidebarMode 全局状态移除）；
    底部选择条"打 Tag"改为"编辑 Tag"批量对话框：覆盖率三态（全有/部分/无）、
    点击暂存循环（添加/移除，应用前不写库、取消零副作用）、单输入框同时承担
    搜索/自动补全/新建（逗号与换行可批量新建）、最近使用 Tag 置顶（settings
    表持久化，上限 10 条）、一次应用产生单条撤销记录；filtered 全选先固化为
    明确行 ID 再打开，暂存期间选区不漂移。
  - 新增 Tag 重命名：Rust `rename_tag` 走 UPDATE 路径（关联经 tag_id 跟随，
    不刷新行更新时间，不扰乱"最近更新"排序），重名冲突中文报错；侧栏 Tag
    右键菜单加"重命名"入口，成功后同步筛选条件并支持撤销/重做。自动规则
    JSON 中引用的旧 Tag 名不随改（对话框中已提示）。
  - 自动规则的规则列表重设计：灰卡两行控件改为干净列表行（序号 + 名称/副行 +
    迷你开关），上下移按钮 hover 淡入，排序变化走 flip 动画；新增全局
    `.switch` 迷你开关样式（32×18 墨黑轨道）。
  - 验证：Rust 全量测试 234 项通过（新增 5 项 rename 测试）、Clippy 严格、
    前端检查 0 错误。
- 性能审查后的第一批修复（分支 `agent/minimal-ui`）：
  - **长任务不再持有全局状态锁**：规则预览/执行、快速打标/分组/画师前缀等
    全库扫描命令改走 `with_cloned_database(_mut)`——克隆数据目录后立即释放
    状态锁，在独立连接上执行，完成后仅短暂持锁失效查询缓存。此前这些任务
    虽已异步化，但持锁期间任何查库操作（如滚动画廊）都会把主线程阻塞在锁上。
  - **导入落位复制阶段可取消 + 有进度**：新增 `Copying` 进度阶段（前端文案
    "正在复制图片进资料库 N / M"），逐张响应取消；此前该阶段进度条假死在
    "感知哈希 100%"，取消要等全部复制完才生效。
  - **"更新现有图片"真正可取消**：更新管线的元数据/哈希/pHash 三个并行阶段
    换用 `parallel_map_cancellable`，副本刷新循环逐张检查取消，写库前有最终
    检查点；`update_existing_images` 命令入口调用 `cancel::begin()`。此前
    取消按钮对更新任务显示但完全无效（按钮永远停在"正在取消…"）。前端对
    更新取消给出温和提示（不作为错误）。
  - 验证：Rust 全量测试 229 项通过，`cargo check` 无警告，`svelte-check`
    0 错误。
- 完成 UX 交互重构四个阶段（分支 `agent/minimal-ui`，与 UI 视觉重构并行推进）：
  - **阶段1 数据安全**：模态浮层打开时全局 Delete/Ctrl+A/Ctrl+Z 短路（modal-layer
    模态栈）；右键未选中项时选区收缩为该项（资源管理器惯例）；全选统计的
    await 前后版本校验消除竞态；排序切换重置 Shift 锚点；删除分组走确认对话框
    （文案明确图片不删、回到未分组、可撤销）；快速整理执行前按张数确认；自动
    规则支持未保存草稿直接只读测试（新增 preview_automation_rule_draft 命令），
    条件/任务切换类型与删除前确认；详情面板编辑器脏状态守卫（Esc 确认、切行
    草稿暂存可恢复）；提示词文档保存失败后切换文档需确认；两窗口 onCloseRequested
    关闭守卫（任务进行中/未保存草稿/撤回栈非空时拦截确认）。
  - **阶段2 基础组件**：Modal 加 Tab 焦点陷阱、关闭归还焦点、busy 时 Esc/遮罩
    不关闭、labelledby 透传；右键菜单按视口夹取（底部/右缘自动翻转）+ 方向键/
    Home/End 键盘导航 + 打开聚焦首项；通知系统队列化（最多 3 条右下角堆叠、
    error 常驻手动关、进度与通知共存、不再压住底部选择条）；分组操作静默吞错
    全部转为明确报错（"已将 0 行分配"假成功、重命名无声失败等）；删除 Tag 确认
    显示影响行数 + busy 态防并发；拖入不支持类型给明确提示；更新现有图片预告
    清空撤销栈。
  - **阶段3 长任务**：规则测试/执行、快速整理预览/应用、画师前缀、数据迁移、
    重置全部改 async + spawn_blocking，不再卡死窗口；parallel_map 新增可取消
    版本，导入管线各阶段边界响应取消（写库开始后不再中断）；新增
    cancel_current_task 命令与导入进度 toast 取消按钮；工具箱三大工具并入
    app.busy 互斥防止并发写库。
  - **阶段4 交互惯例**：Ctrl+单击图片加选/取消、Shift+单击范围选（单击仍是看
    详情）；Shift 跨未加载页时明确提示实际选中数；缩略图"取消≠失败"（滚出视口
    不再永久显示"不可用"，真失败可点击重试）；外拖后死点击修复；空状态按实际
    筛选维度生成文案 + 一键清除；两窗口输入框放行原生右键菜单（粘贴可用）；
    工具箱窗口聚焦/主窗口广播时刷新快照（工具可用性不再冻结）；导出工具改为
    无库可用；首屏支持拖放导入；重复项/分组卡片显示文件名 + 分辨率。
  - 验证：Rust 全量测试 229 项通过（新增草稿预览与取消测试）、Clippy 严格、
    前端检查 0 错误、生产构建通过；Playwright 模拟 Tauri IPC 冒烟测试
    阶段1（9 项）/阶段2（7 项）/阶段4（7 项）全部通过。
- 前端完成第三轮结构性重构，主窗口完整对齐"画纸"设计稿（分支 `agent/minimal-ui`）：
  52px 统一标题栏（品牌区 + 分段控件视图切换替代左侧 NavRail + 搜索胶囊 +
  文字风按钮）、画布头部（24px 大标题 + 匹配计数 + 排序/尺寸控件迁入，各视图
  旧工具条删除）、筛选 chips 行（搜索词/Tag/开关逐项可删 + 一键清除，与顶栏
  搜索框双向联动）、侧栏筛选化（check-row 排版 + 生效计数 + AND/OR 文字切换）、
  底部通栏选择条（批量编辑 Tag 对话框、导出所选上弹菜单、取消选择蓝字）、
  详情面板文件名标题 + 分辨率·时间副行；
  另补全局模态层栈（modal-layer），模态打开时资料库级快捷键短路、Esc 分层关闭。
  前两轮的纸感 token（白纸面/墨黑主按钮/系统蓝/柔和大阴影/胶囊控件）与
  画册式卡片全部保留。
- 工具箱窗口完成"画纸"语言精修（7 个 commit）：全局层新增 select/radio 接管
  （appearance:none + 自绘箭头/墨黑圆点）、自绘 .progress 进度条、currentColor
  .spinner、.tool-card/.step-badge/.metric-grid/.action-bar/.sample-grid/.txt-*
  共性组件类；外壳 52px 标题栏 + 24px 画布标题 + 侧栏灰选中态 + 自动规则页
  fullBleed 单头；九个大页面逐页收编全局类，清零硬编码警示色（琥珀/绿系换
  --warning/--success token）、文本字符图标换 Lucide、三份 spinner keyframes
  与四份指标宫格实现合一；维护页补上感知哈希进度条渲染。
- 完成一轮"轻盈优雅"动效补全（6 个 commit）：修复被 scoped display 覆盖而失效的
  工具箱面板切换动画（改为 display 重放的一次性进场 keyframe，220ms 淡入+7px 位移）；
  主窗四视图与提示词文档切换加 180ms 进场；缩略图异步加载淡入（缓存命中同步路径
  不淡入防闪）、高清层 visibility 硬切换为 opacity 交叉淡入；ViewSwitcher 增加
  白色胶囊滑动指示器（ResizeObserver 跟随计数变化与窄窗口媒体查询）；筛选 chips
  进出场、右键菜单 transform-origin 跟随翻转方向、toast 栈 flip 让位；新增
  flipDuration() 统一让 animate:flip 尊重 prefers-reduced-motion（替换 3 处裸
  flip）；2 处硬编码时长 token 化；详情面板开合从瞬时挤压改为 panelSlide 宽度
  滑入（内衬层防内容挤压，宽度 tier 提为 --detail-width 变量）。
- 详情面板"生成信息"kv 表已上线（模型/采样器/步数 CFG/种子，Rust 暴露五字段）。
- 工具箱快速整理的画师前缀操作已支持跨逗号的 NovelAI 数值权重区段；例如输入
  `rourow` 可将 `1::artist:huangdanlan, rourow::,` 正确改为
  `1::artist:huangdanlan, artist:rourow::,`，同时保留结束符与原始空格。
- 资料库排序按钮补回单一下拉箭头，并在菜单展开时旋转，明确提示该按钮可选择。
- 资料库排序控件已进一步精简：移除顶部浏览说明、菜单项装饰图标和按钮内的
  “排序方式”副标题，按钮仅显示当前排序名称。

- 主程序当前可用。
- 当前数据库 schema 为 v16；新库不再依赖 Excel，XLSX 仅作为导出格式。
- 本次发布版本为 0.12.2（GitHub Release `v_28`）。

## 已交付能力

### 图片资料库

- 支持从单张 PNG、文件夹、ZIP、7z 和 RAR 导入 NovelAI 图片。
- 解析 PNG 文本块及 NovelAI Alpha 通道隐写中的正向提示词、角色提示词、
  负向提示词、画师串、VIBE 引用和完整元数据。
- 通过来源身份、SHA-256 和元数据指纹识别重复内容，并可重新关联搬家或重新保存
  后的原图。
- 使用 SQLite 保存图片记录、Tag、分组、备注和导入信息。

### 浏览与整理

- 提供虚拟化画廊和表格，支持搜索、Tag AND/OR 筛选、分组、批量选择和批量编辑。
- 画廊和表格顶部提供共用排序选择，支持时间正序、时间倒序与最近更新；新导入、
  提示词编辑、备注、Tag 增删及分组变更会进入最近更新时间排序。
- Tag 库可勾选“筛选存在 VIBE 的图片”；该条件覆盖分页、全选、批量编辑 Tag、重复项
  浏览和当前结果导出。旧资料库首次打开时会自动建立 VIBE 数量索引。重复项视图
  可“按 VIBE”聚合共用同一组引用的图片，组名支持别名重命名。
- 支持提示词、备注、Tag 和分组操作的会话级撤销/重做；图片删除和更新现有图片
  等高风险操作不进入撤销历史。
- 画廊依次使用 256px 缩略图、1024px 高清图和 2048px 详情预览，全屏查看时
  按需读取原图。
- 画廊卡片显示 VIBE 数量；详情支持提示词、备注、Tag、图片信息和原图预览。

### 工具箱

- 独立工具箱窗口承载自动规则、随机画师串、以图搜图、智绘姬 JSON 去重、感知哈希维护、
  失败图片目录、数据管理、图片导出、全库快速整理和画师前缀修正。
- 自动规则是与快速整理分开的持久化功能，可在没有资料库时预先编写。规则支持启停、
  排序、导入/更新触发开关、两层 AND/OR 条件组及整体“条件成立/不成立”；条件覆盖
  提示词、Tag、分组、画师、备注、文件信息、VIBE、元数据、尺寸构图和完整 NovelAI
  生成参数。任务支持 Tag、分组、提示词、画师前缀、备注及按图片停止后续规则。
  正式新库不包含任何预置规则；角色固定形态与 Tag 的示例只存在于回归测试中。
- 自动导入规则只处理本次真正新增的行，重复图片不会触发；更新规则只处理本次成功
  更新的既有行。每条规则使用独立事务，单条配置或执行失败会在导入结果中报告，
  不回滚已导入图片，也不阻断后续规则。现有资料库可先只读预览命中/改动数和示例，
  再确认手动应用；产生修改时清空旧撤销/重做栈，避免历史操作覆盖规则结果。
- 自动扫描正向、角色和负向提示词中的完整裸 Tag，保留 NovelAI 权重与括号，跳过
  已有 `artist:` 前缀和相似名称。只有资料库其它位置已明确写成 `artist:名称` 的同名
  裸 Tag 才会成为候选，因此常用词和无法由库内证据确认的名称不会被误判。候选支持
  搜索、逐项选择及在主窗口查看示例；应用后重算正向/角色来源的画师串，支持会话级
  撤回与重做，且不读取或改写原始图片文件。
- 扫描会先收集全库正向、角色和负向提示词中已经明确使用 `artist:名称` 的 Tag，
  将它们作为“库内已有明确标注”的最高优先级证据。其它图片中完全匹配的同名裸 Tag
  会默认勾选；仅名称相似的内容仍然跳过。
- 快速整理支持按正向提示词、角色提示词、负向提示词、画师串与备注中的严格
  AND 组合扫描整个资料库，预览后批量添加多个 Tag 或移入指定分组；Tag 可在
  工具内直接新建，临时匹配规则不入库。批量分组可勾选“仅处理未分组的图片”，
  命中但已有分组的图片会被统计并跳过，不会离开原分组。
- 快速整理“提示词操作”支持输入单个画师名，在正向、角色与负向提示词中批量
  补全 `artist:` 前缀；仅修改完整匹配的裸 Tag，保留括号及 NovelAI 权重语法，
  包括跨逗号、以 `::` 结束的数值权重区段；跳过已有前缀和相似名称，并提供影响预览、
  示例图片、撤回与重做。画师串只从
  修改后的正向和角色提示词重算，负向画师不会进入筛选用画师串。
- 提示词匹配忽略大小写和 NovelAI 常见权重语法，并将 `girl`、`1girl`、`1 girl`
  作为同一个泛用标签；其它内容仍严格区分内部空格、下划线及字符。执行使用
  单事务，并按实际新增关联提供精确撤回与重做。
- 点击以图搜图结果会恢复并聚焦主窗口，清除遮挡目标的筛选条件，再把对应图片
  定位到画廊中央。
- 图片导出工具实时读取主窗口选区，也可点击选择本地文件夹，或把多张图片、多个
  文件夹及其混合内容拖入指定区域；文件夹递归扫描子目录，按完整路径追加去重并
  按文件名自然排序。保留原名、随机乱码、自定义前缀序号及导出副本元数据清除等
  既有选项不变；PNG 会重新编码并清洗 NovelAI Alpha 最低位隐写载荷，输出直接
  写入用户选择的文件夹并自动避让同名文件，不修改原图或资料库。
- 分组管理保留在分组视图内，不放入通用工具箱。

### 导出与维护

- 支持将当前筛选或选中结果导出为带缩略图的 XLSX、智绘姬 JSON 或图片文件包。
- 工具箱可将主窗口明确选中的图片导出到指定位置，并按需重命名或重新编码清除
  导出副本的元数据。
- 支持数据目录迁移、资料库重置、感知哈希刷新和失败图片检查。
- 更新现有图片只更新已匹配记录，不新增图片，并保留用户的 Tag、分组和备注。

## 最近有效变更

- 分组与重复项视图补齐画廊同款多选：成员卡片新增悬停/选中复选框和选中描边，支持
  Ctrl 加选、Shift 跨当前展开区段连选、右键保留批量选区及缩略图外拖；底部可直接
  批量编辑 Tag、移入分组、编辑提示词、导出和删除。Ctrl+A 与“全选”会按当前视图
  补取折叠或未加载成员，重复视图不会误选唯一图片；跨不同数据集合或切换重复依据时
  清空旧选区。前端检查和生产构建通过（保留 3 条既有图片拖动可访问性警告及既有主包
  体积提示）；Playwright 验证分组 Ctrl 多选/全选、重复项跨区段 Shift 连选、右键批量
  目标、Ctrl+A 范围及 760×520 选择条布局。
- 图片右键菜单新增“只看当前画师串”：从画廊、表格或分组成员触发后进入普通画廊/
  表格浏览，清除其它筛选并精确显示全库中完整画师串相同的图片；顶部提供可单独关闭的
  画师串筛选标签。筛选条件已贯通分页缓存、全选当前结果、批量操作与当前结果导出；
  新增精确匹配、首尾空白归一、与文本搜索组合及筛选全选范围的后端回归测试。
  Rust 全量测试 239 项通过、1 项性能基准忽略，Clippy `-D warnings`、前端检查与生产
  构建通过（保留 3 条既有图片拖动可访问性警告及既有主包体积提示）。
- 新增“导入时自动补全画师前缀”持久化开关：首次导入页和顶部导入菜单均可设置，
  默认关闭。开启后在自动规则执行完毕后，仅扫描本次新增图片，依据全库明确
  `artist:` 标注直接补全同名裸 Tag；导入进度持续显示无需确认的提示，完成通知报告
  扫描与修改数量。失败不会回滚已导入图片，并新增设置、指定行隔离和真实图片导入测试。
  Rust 全量测试 228 项通过、1 项性能基准忽略；前端检查和生产构建通过。
- 移除外部画师词典、随包快照、联网更新入口及发帖量/别名/冲突判定。画师前缀修正
  现在只接受资料库内已有的明确 `artist:` Tag 作为证据；没有证据的裸 Tag 不会显示。
  数据库升级至 schema v15，并在升级时删除旧版遗留的词典缓存表。Rust 全量测试
  225 项通过、1 项性能基准忽略；前端检查与生产构建通过（保留 3 条既有图片拖动
  可访问性警告）。
- 自动规则的列表型输入新增全角逗号防呆：Tag、提示词组合和画师名现在统一接受
  半角逗号 `,`、全角逗号 `，` 或换行；界面明确标注支持范围，并对 Tag、画师列表
  即时拆分，后端在校验、匹配、执行和自动创建 Tag 时再次解析，旧数据或直接接口调用
  也不会生成带全角逗号的组合 Tag。顿号 `、` 不作为分隔符，仍可用于名称本身。
  新增端到端数据库回归用例。
- 新增通用自动规则引擎和独立规则编辑器：数据库升级至 schema v14，持久化规则顺序、
  触发时机、两层条件组和任务列表，并缓存规则所需图片尺寸及 NovelAI 生成参数。
  导入仅对新增行执行，更新仅对成功更新行执行；规则逐条事务隔离并输出执行报告。
  工具箱支持新建、编辑、启停、排序、删除、只读预览和确认应用，首次导入页面可直接
  打开规则编辑器。用户提供的“固定形态 → 添加 Tag”只作为动态创建的集成测试，
  源码、迁移和新库均无硬编码预设。Rust 全量测试 225 项通过、1 项性能基准忽略，
  Clippy `-D warnings`、前端检查、生产构建及隔离目录 Tauri debug `--no-bundle` 构建通过；
  Playwright 完成组合条件、多任务、保存、预览、确认应用及 900px 最小宽度验证。
- 修复快速整理画师前缀操作无法识别跨逗号数值权重区段末尾 Tag 的问题：按照 NovelAI
  `数字::内容::` 语法，将末尾无数字的 `::` 作为权重结束符而非画师名的一部分；支持
  `rourow::` 与官方示例风格的 `rourow ::`，修改时保留原始空格和结束符，并同步重算
  画师串。新增快速整理完整预览/应用回归测试及底层手动、自动前缀解析测试。Rust 全量
  测试 213 项通过、1 项性能基准忽略，Clippy 严格检查通过。
- 重做画廊与表格的排序菜单：移除系统原生下拉框，改为带当前模式图标、简短说明、
  清晰选中态的自定义弹出菜单；加入弹出缩放淡入、箭头旋转、图标与选项悬停反馈，
  同时支持点击外部关闭、Esc、方向键/Home/End 导航和减少动态效果偏好。窄窗口下
  触发按钮会收进视图自身宽度，不再覆盖侧边栏。Playwright 在 1280×800 和
  760×520 下验证画廊、表格、模式切换、菜单关闭与响应式布局。
- 为画廊和表格增加时间正序、时间倒序、最近更新三种共享排序。数据库升级至
  schema v12：新行以导入时刻初始化更新时间，历史行用原批次导入时间回填，
  提示词、备注、Tag 与分组的实际变更由触发器自动更新时间，内部哈希和图片状态
  维护不会扰乱顺序；单行编辑在最近更新模式下会立即刷新分页并将目标移到前面。
  “最常用”按本轮决定暂不实现。新增排序、定位、编辑触发和 v8/v9 升级回归测试；
  前端检查与生产构建通过，Rust 全量测试 195 项通过、1 项性能基准忽略，Clippy
  严格检查通过。
- 为工具箱图片导出增加本地批量选图入口：点击拖放区打开系统文件夹选择器；拖放
  支持单张/多张图片、单个/多个文件夹及混合输入。后端递归扫描所有子文件夹，
  支持 PNG、JPG/JPEG、WebP、GIF、BMP、TIF/TIFF，按规范化完整路径追加去重，
  再按文件名自然排序；新增来源与主窗口选区统一进入原有导出管线，重命名、重名
  避让、并发导出和元数据清除行为保持不变。界面显示追加数量、少量文件名预览，
  并允许单项移除或一键清空。新增递归扫描、自然排序、重复路径以及空主窗口选区
  导出外部图片的回归测试；前端检查与生产构建通过，Rust 全量测试 193 项通过、
  1 项性能基准忽略，Clippy 严格检查通过。
- 修复“筛选单画师串图片”偶尔混入多画师图片：快速画师前缀修正统一将重算结果
  保存为逐行画师串；资料库首次打开会自动修复历史的逗号分隔记录，无需重新导入
  原图；筛选查询也会直接排除尚未修复的逗号多画师串。新增旧库自动修复及新旧格式
  筛选回归测试，全量 Rust 测试 191 项通过、1 项性能基准忽略，Clippy 严格检查通过。
- 引入开源 `@lucide/svelte` 图标库，将主导航与尺寸滑块的手绘 SVG 迁移为
  按图标入口加载的组件；参考“花笺”的克制线性风格，主导航统一为 18px，
  工具箱入口为 15px，窗口控制为 13px，并改用更直接的资料、列表、文件夹、
  搜索和设置语义图标；最小化、最大化/还原和关闭按钮也统一使用 Lucide，
  仅应用品牌图标继续保留专用 SVG。工具箱七个工具入口同步从字符符号迁移
  为语义化 Lucide 图标。前端检查和生产构建通过，Playwright 模拟 Tauri
  运行时后完成主窗口与工具箱视觉核对。
- 稳定 Windows MSVC 开发构建：仅将应用 crate 的 Debug codegen 单元收敛为 1，
  保留增量编译和依赖并行构建，避免 `tauri dev` 链接 cdylib 时出现 LLVM 匿名
  符号 `LNK2019`；全量测试后再次执行普通开发编译已稳定通过。
- 为快速整理的批量分组增加“仅处理未分组的图片”选项；预览单独显示跳过的
  已分组图片，执行、通知与结果均报告实际加入及跳过数量，撤回只恢复实际改动。
- 为快速整理条件增加首组泛用标签别名：`girl`、`1girl`、`1 girl` 双向等价，
  同时保持 `2girls`、`girlfriend` 等相似标签不命中。
- 统一快速整理目标列表的选中样式：分组仍为单选，但改用与 Tag 相同的方形
  `✓` 勾选反馈，移除视觉不一致的圆点样式。
- 优化快速整理的 Tag 与分组新建入口：各自搜索框右侧改为按需展开的“＋ 新建”
  按钮，点击后显示名称输入框及创建/取消操作，创建成功后自动收起并选中新目标。
- 开放快速整理“提示词操作”，完成全库画师前缀修正：一次输入一个画师名，
  同时处理正向、角色、负向提示词，并按图片及提示词字段统计影响范围；执行和
  撤回均使用单事务，精确保存三类提示词和画师串的修改前后状态。
- 完整退役电子表格导入及旧工作簿运行时依赖，保留生成式 XLSX 导出。
- 工具类功能迁入独立工具箱窗口，主窗口聚焦资料库浏览、导入和导出。
- 完成以图搜图结果到主窗口画廊的跨窗口定位。
- 将“快速编辑”更名为“快速整理”，新增工具内新建 Tag 和“提示词组合 →
  指定分组”动作；分组执行会逐张记录原分组，撤回恢复各自原状态，重做再次
  移入目标分组，并继续为提示词替换、追加和删除动作保留复用结构。
- 完成工具箱图片导出：同步主窗口选区，支持保留原名、随机名、自定义序号命名
  和仅对导出副本重新编码清除元数据。
- 修正 PNG 导出副本的元数据抹除：除文本、EXIF、ICC、时间、物理尺寸、颜色
  建议和私有附加块外，同时清除 NovelAI 写入 Alpha 通道最低位的
  `stealth_pngcomp` 隐写载荷；移除导出时逐张二次解码复检，改由自动化测试验证
  普通块与隐写层均已清除，缩短批量导出耗时。
- 优化大批量图片导出：改为并行流水线，按机器逻辑核心数与本批最大图片的估算
  解码内存共同限流（最多 8 个编码任务，纯复制最多 4 个），PNG 使用快速压缩和
  `Sub` 滤镜；开发版启用最低优化级别，避免 Debug 编解码慢几十倍。真实 56 张、
  约 2.70 亿像素样本由约 160.7 秒降至 2.218 秒，内置检测器复检 56 张均无元数据。
- 表格导入检测器同步支持 Alpha 隐写元数据，避免把只有隐写载荷的新式 NovelAI
  图片误判为无元数据。
- 验证：Rust workspace 库测试 193 项通过、1 项性能基准忽略；Clippy
  `-D warnings`、前端检查和生产构建通过；Playwright 在 960×680 与
  760×520 下完成快速整理 Tag 新建、批量分组预览及无横向溢出，并检查导出
  工具选区同步、重命名和元数据开关。
- 完成画廊渐进式高清加载、分层缓存和 VIBE 徽章。
- 完成 VIBE 图片筛选及 schema v11 数量索引；新导入和更新现有图片实时写入，
  v8–v10 资料库自动升级并回填历史图片。
- 修复分组与重复项视图的卡片未显示 VIBE 数量徽章；两种视图现与画廊共用
  同一 VIBE 状态缓存并按卡片加载引用数量。
- 完成原图内容哈希 / 元数据指纹重关联与受管原图可信标记。
- 修复工具箱“自动规则”规则列表的启用开关圆点错位：覆盖 checkbox 基础样式
  遗留的水平居中规则，使关闭与开启状态分别准确贴合轨道两端。

## 当前待办

1. 重新确定正式界面的视觉与信息架构方向。
2. 补充资料库备份、恢复和数据库健康检查入口。
3. 改善原图重关联出现多个候选时的人工选择流程。
4. 继续优化大资料库筛选、图片缓存淘汰和批量操作性能。

## 验证基线

- 前端：`npm run check`、`npm run build`。
- Rust：workspace 库测试、Clippy `-D warnings`、Tauri debug `--no-bundle` 构建。
- Native 文件对话框、安装包覆盖安装和智绘姬 JSON 实际加载仍需发布前人工抽查。

## 已知约束

- v8–v15 数据库可自动升级到 v16；v1–v7 需要先使用旧版本升级。
- XLSX 只允许生成式导出，不读取、修改或依赖旧工作簿。
- 不安装全局 `cargo-tauri`，使用仓库内 `@tauri-apps/cli`。
- Node 版本保持不变。
