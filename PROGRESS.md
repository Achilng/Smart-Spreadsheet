# 当前进度

最后更新：2026-09-15

> 只保留当前交接信息，目标不超过 40 行。更新时替换旧状态，不追加开发流水；已完成事项在发布后移出，历史查 Git 和版本更新说明。

## 当前状态

- 「素材」分区已完成、待发布：图片卡片、单段文本、共享 Tag、单击详情/编辑、双击复制，支持图库选图、本地文件选择与拖入后逐张确认、PNG 元数据多区域选择。
- 素材与展示图随数据库保存，schema 升至 v19；空资料库可进入主窗口；共享 Tag 删除后的撤销同时恢复素材关联。
- 素材卡片改为明确的可选样式：内嵌圆角封面、右上角勾选标记、选中底色、悬停反馈与柔和 Tag，保留单击详情和双击复制；前端类型检查与构建通过，外观由用户验收。
- 仓库记录的最近发布为 `v_40`，应用版本 `0.12.14`；发布流程见 [docs/RELEASING.md](docs/RELEASING.md)。
- `v_40` 之后已完成、待下次发布：随机画师串输出稳定与动效、XML `<artist>` 块解析及历史数据修复、主窗口浏览历史前进/后退。

## 验证与未结项

- 本次验证：24 项前端单元测试、类型检查和构建通过；297 项 Rust 测试通过，3 项手动性能测试按原设置跳过。包括 v18 升级、素材备份后重开读取、共享 Tag 撤销恢复及真实 PNG 只读预览。
- 浏览器（模拟 IPC）验证通过：单击/双击、编辑/更换封面、搜索/Tag/分页、元数据多选、逐张导入/取消、保存失败保留草稿、删除确认、过时查询淘汰与快捷键隔离；默认及 900×600 布局已目视检查。
- 素材选图已改为直接进入现有画廊：移除弹窗图片列表，保留草稿，画廊单选后确认返回；沿用完整搜索、Tag 筛选、缩略图和虚拟滚动。前端类型检查及构建通过（保留原有 3 条可访问性警告），交互由用户验收。
- 用户已接手实机鼠标检查，本次停止桌面自动操作；原生文件对话框、资源管理器拖入和图库选图交互由用户验收。

- 浏览历史：22 项前端单元测试、前端检查及构建通过；模拟大库和 Windows 实机浏览验证通过。
- 待验证：实体鼠标侧键在 WebView2 中的前进/后退行为，目前只有事件模拟验证。
- 已知构建提示：3 条图片可访问性警告及包体积提示，尚未清理。
- 下次发布前人工抽查：原生文件对话框、安装包覆盖安装、智绘姬 JSON 实际加载。

## 维护方式

- 开工时写清当前任务、下一步和阻塞；收尾时只留结果摘要与未结项。
- 不维护完整功能清单，不重复 PLAN.md 的候选方向，不保留逐次测试数量、耗时或操作日志。
- 只保留与当前交接有关的验证结论，注明未验证范围；发布后移除已发布变更摘要。
