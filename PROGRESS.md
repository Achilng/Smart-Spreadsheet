# 当前进度

最后更新：2026-09-24

## 当前状态

- 人工复核后将提取提示词升级为 style-extraction-v2：尊重画师段与人物段之间的空行，不吸入下一段质量词；向前保留同段水墨、水彩、线稿、黑白等画风描述，补充分段、媒介和构图边界示例。用户澄清第 62 条提取正确（人工误判），第 64 条属可接受歧义，不用于调整。未改原结果或浏览器判定；15 项现有程序测试、8 个提示词示例的连续原文检查通过，尚未进行真实模型准确率复测。确认无在途调用后重启外部服务加载新版，旧任务遵循原有提示词哈希保护，使用原请求文件创建新任务采用新版。

- 修复模型编号抄写导致整批失败：API 与 Codex CLI 请求统一使用每批从 1 开始的短编号，结构化输出枚举该批合法编号；SHA256 仅留在程序内部及交换文件。返回按编号映射，支持乱序，未知 / 缺失 / 重复编号逐条隔离，已通过项不重跑。提取提示词未改，旧任务可直接重试。15 项测试通过，新增覆盖短编号出站、乱序映射及 10 条中 1 条错编号只重试 1 条；原失败的第 81–90 条真实 API 复测 10 条全部通过格式与连续原文校验，无未知编号。验证结果保存于 D:/Agent/Agent_temp/style-extractor-short-id-check.json，不覆盖原任务结果。

- 根据用户后续要求，API Key 改为按 Base URL 使用 Windows DPAPI 当前用户加密保存至外部工具 data/.credentials.json（已忽略，不进入 Git）。填写、离开输入框或开始任务会保存，刷新 / 重启 / 切换任务自动回填匹配端点的 Key，支持手动保存及清除。异步保存和删除串行处理，防止删除后被未完成保存恢复。此前提供的 Key 已加密保存。13 项提取工具测试通过，包含加密落盘、重载、端点隔离和并发保存后清除；原有任务数据与日志中仍不包含凭据。

- 新增独立开发者批改台 tools/style-review，端口 17324，只读载入指定结果文件。原文高亮与模型结果对照，正确 / 错误 / 待定、快捷键、筛选搜索、备注和可空的人工参考答案；自动保存浏览器进度，导出 / 恢复批改记录，错题导出 TXT。准确率仅计算已判对错样本，未批改 / 待定 / 调用失败单列。当前文件 123 条中 113 条可批改、10 条调用失败。2 项逻辑测试及浏览器判定、刷新恢复、导出再导入验证通过；测试标记已清除，原始结果未改写。

- 外置画风提取新增并发请求数（1–32，默认 1）：与每批条数独立，10 并发 × 每批 10 条最多同时处理 100 条。旧任务默认 1，可在右侧调整后开始 / 继续 / 重试，无需新建；显示进行中的请求数，并发返回独立落盘且导出顺序不变。暂停或额度 / 限流错误取消全部在途请求，等待退出后释放任务，续跑跳过已保存项。12 项外部测试通过，覆盖 10 并发上限、乱序、暂停 / 限流后恢复、局部失败重试与旧任务兼容；浏览器使用本地模拟 API 验证 10 改 3 后峰值为 3，10 条完成，390px 无横向溢出。未额外消耗真实模型调用。待用户原任务完成后已重启外部服务加载新版，智能表格 dev 保持运行。

- 2026-09-24 按用户要求启动持续运行的 `npm run tauri -- dev`：当前代码完成 dev 编译（3 分 38 秒），`target/debug/smart-spreadsheet.exe` 开发窗口已打开并响应；Vite 1422 与 Tauri 文件监听保持运行，前端热更新、Rust 修改自动重新编译。原独立 Vite 进程已交由 Tauri dev 管理，正式安装 exe 未替换。

- 已实现外置 LLM 画风提取：工具箱新增「LLM 画风提取」，按全部 / 选区 / 筛选范围导出正向原文，完全一致才去重；结果按哈希和全文预览导入，同一正文的记录一起更新。数据库 v21 持久记录来源，表格与详情显示 LLM 标记；成功空结果同样有效，失败不覆盖，正向正文改变后失效，角色 / 负向 / 备注变化保留，支持撤销与重做。自动重算与启动修复路径不会覆盖有效 LLM 结果。
- 外部 tools/style-extractor 提供本地网页、进度条、累计运行时间、每次调用数量（默认 10，可设 1–100）、有限重试、暂停与断点续跑、断线自动重连。任务文件保存原文、结果和参数，Key 只留运行内存；已完成项不重跑。默认 API 地址 https://oneapi.hakoyu.com/v1，模型名 gpt-6-luna【神秘】，同时保留 Codex CLI 模式。Base URL、模型名均可编辑，API Key 需自行填入，不写入日志 / JSON / Git。启动方式及细节见 tools/style-extractor/README.md，协议见 docs/LLM-STYLE-EXTRACTION.md。
- 本轮验证：前端类型检查、生产构建、38 项既有单元测试通过；Rust 304 通过、3 项原有性能测试忽略；外部工具 7 项测试通过，覆盖原文校验、分批、暂停续跑、API 兼容参数回退与凭据不落盘。真实 API 调用的原始 3 个示例逐字一致；单批 10 条（原始 3 条及 7 条小样本）约 11 秒完成，6 条有画风、4 条无画风，全部与预期一致。该结果不代表全库准确率，第三方底层模型身份未验证。
- 已检查外部网页 1200px / 390px 和工具箱模拟页 900×600 布局，服务重启后自动重连恢复，Key 为空。外部正式服务已在 http://127.0.0.1:17321 启动，测试数据隔离在 D:/Agent/Agent_temp。Codex CLI 的 ChatGPT 登录端点实测拒绝 gpt-6-luna，API 模式已可用。未替换当前运行的正式 exe、未操作用户真实资料库，原生对话框与真实跨窗口流程不宣称已验收。

- 2026-09-22 已将易读性版本（代码 82081af）构建为 Tauri release 程序并替换 D:\应用\智能表格\smart-spreadsheet.exe；版本号保持 0.12.14，未发布 Release。安装文件与构建产物 SHA-256 一致：FFE4520F8D55303461D6AABDBFDE58F3FA89B513C65B1439B8E836442DD15A02。旧程序已备份并校验至 D:\应用\智能表格\backups\20260922-225208-before-typography\smart-spreadsheet.exe。未改动资料库，未重新启动正式程序，当前开发进程保持运行。

- 易读性第一版：参考 Apple Typography，提高主窗口、素材、工具箱和对比窗口的辅助文字与控件字号，共享阶梯改为 13 / 14 / 15 / 17 / 20px；详情提示词为 15px 正文，主要标题与选中项采用 600 字重，保留微软雅黑和素材名称宋体。辅助灰字从 #86868b 改为 #63636b，白底对比度由 3.62:1 提升至 5.95:1，浅灰底最低约 5.23:1；链接和成功色同步加深。卡片文字行高为 21 / 20px，虚拟列表页脚占位从 42 调整为 50px。
- 改动前执行 git add -u，已跟踪文件原本干净；建立 backup/typography-before-20260922 回退分支，基线 6f316e1。未跟踪的 .claude 工作树未纳入提交。类型检查、生产构建和现有 38 项测试通过；浏览器模拟页已检查 900×600 画廊 / 表格 / 详情 / 工具箱自动规则 / 对比首页及 1440×900 画廊布局，工具箱 JSON 导入导出按钮已按窄栏自动分行；不等同于原生窗口实机验收。

- 素材卡片复制成功浮层按用户要求移除前置勾号，仅显示“复制成功”，沿用现有玻璃效果。

- 已按用户要求将当前 React 界面字体统一为微软雅黑（Microsoft YaHei），覆盖数字、Tag 数量、按钮、输入框及文本预览；移除 Segoe UI / 微软雅黑 UI 优先级。主窗口、工具箱及对比窗口共用设置，类型检查与生产构建通过。素材卡片名称按用户最新要求恢复原来的华文宋体 / 宋体，其余界面仍用微软雅黑。

- 素材版本总数采用 huahui-count-options.html 的 D 方案：卡面右下角显示叠卡轮廓和数字，只有一个版本也显示 1，切换版本或搜索目录不改变总数。名称与数量分列，长名称最多两行，不遮挡数量；不增加悬停提示。

- 已按用户要求移除卡片下方的版本名、横线及箭头，并去掉对应空白高度；版本目录继续通过右键或 Shift+F10 打开；目录顶部的“版本目录”标题与数量也已移除，列表直接从上方开始。卡面、名称及版本选项的悬停文字提示已移除，复制反馈保留。

- 卡片复制反馈已按用户指定接入 [rdev/liquid-glass-react](https://github.com/rdev/liquid-glass-react)，锁定 npm 1.1.1：使用 standard SVG 折射、轻色散、150% 饱和度与 14px 圆角，叠加乳白薄层并沿用微软雅黑深灰文字。局部适配库的默认居中定位与字体，高光层按实际浮层尺寸定位，避免初次显示从默认 270px 宽度缩小。提示保持卡面 42% 位置、1.8 秒后淡出，220ms 后卸载滤镜和 resize 监听；关闭提示的鼠标跟随，保留减少动态效果 / 透明度及强制色彩回退。复制失败、空文本和版本点击复制共用此浮层，过期请求与卸载保护沿用原实现。MIT 声明随 `public/THIRD-PARTY-NOTICES.txt` 打包。
- 本轮玻璃接入验证：类型检查、生产构建及现有 38 项单元测试通过。900×600 浏览器模拟素材库确认复制成功提示、SVG 滤镜引用、高光尺寸对齐及消失后滤镜 / 组件清理；冷色底图的完整画面在减少动态效果模式下暂停瞬时提示核查，验证后恢复浏览器设置。开发服务已加载库与新样式，未新增测试文件，未做 Tauri 原生窗口实机验收或全库性能测试；不宣称与苹果原生材质逐像素一致。
- 素材卡片按 `D:\Agent\playground\huahui-card.html` 重构，保留白色页面：832:1216 竖图、名称叠字、悬停浮起 / 倾斜 / 跟随光效及绿色选中描边。右键展开目录，在原卡位内缩小左移，同时只展开一张；编辑和删除继续使用右侧详情。
- 点击版本列表中的任意项，立即切换图片并复制该项文本，随后收起列表并显示卡面复制反馈；再次点击当前项也会复制。无独立图片沿用素材封面，双击卡面仍可复制当前版本文本；版本选择保存在浏览状态，跨虚拟滚动保留，不改变默认版本和保存顺序。超过 8 个版本提供搜索，支持长名称、方向键 / Enter / Escape、Shift+F10 和减少动态效果设置。版本图片使用独立的有界缓存，避免与详情的小容量缓存互相淘汰。
- 本轮验证：类型检查、生产构建和现有 38 项单元测试通过；Edge 模拟素材库通过 1440×900 / 900×600 画面检查、双击文本校验、封面回退、同一时间单目录、相邻卡位不变、滚动后版本保留、最小尺寸 12 版本搜索及键盘操作。未新增仓库测试文件；未宣称真实资料库 / 原生窗口验收，未发布 Release 或替换正式程序。

- Tag 的 AND / OR 切换已改为灰底、白色选中项的双选分段控件，支持键盘方向键和含义提示；单独更改组合方式时保留当前滚动快照，不再按新增筛选处理并跳回顶部。结果缩短时停在新的可用末尾，清除筛选仍恢复筛选前位置。
- AND / OR 验证：无 Tag、单 Tag、多 Tag 分别保持 19,000px、14,000px、11,000px；两 Tag 的 OR / AND 结果数正确（45,000 / 15,000），列表末尾自动钳制、表格 12,000px 位置保留、键盘切换与清除筛选返回 19,000px 均通过。1180×760 和 900×600 布局正常，类型检查、前端构建及现有 38 项单元测试通过；未新增测试文件。
- 针对“滚动时少数已有图片仍闪烁”补充修复高清切换：复现了高清请求在滚动中完成时，原元素虽未重建，但 `src` 已改为尚未解码的高清图。现在缩略图常驻底层，独立高清层通过 `decode()` 后再显示；解码失败保留底图，卸载时丢弃过期解码回调。缩略图组件使用 `memo`，避免仅滚动导致的重复渲染。
- 保留此前滚动与筛选修复：取消图片反复淡入，离开可见区不再主动降为缩略图；缓存读取区分加载器、行 ID 和换库版本。虚拟列表在新结果首次渲染时即使用目标滚动位置，并区分占位卡片与实际图片的 key。
- 本轮验证：类型检查、生产构建、现有 38 项单元测试与 Tauri dev 编译通过，未新增测试文件。Edge 隔离模拟 6 万行图库，延迟高清请求和解码期间 21 张已有缩略图保持原元素及地址，高清解码失败时底图仍可见；筛选清空后过期解码不会复活旧图片。通过上下滚动、Tag 筛选及清除、详情显示、1440×900 与 900×600 画面检查。此前的跨分页位置恢复、查询失败重试、空结果恢复与连续筛选验证记录仍有效；这些浏览器模拟结果不等同于用户真实资料库和 Tauri 实机验收。
- 已编译含高清层修复与 AND / OR 切换调整的调试程序；未替换 `D:\应用\智能表格` 中的正式程序。用户明确要求的 dev 是持续运行的 `npm run tauri -- dev`：保留开发窗口和文件监听，前端改动热更新，Rust 改动自动重新编译并重启；后续沿用此方式，不再每次打包独立 exe。
- React 主体迁移已收尾：分支 `feat/react-ui-refinement`。保留现有结构、功能位置和白色配色，只精修细节；用户要求不新增测试文件。完整范围见 [专项计划](docs/REACT-REWRITE.md)。
- React 主窗口六个视图、十项工具、图片对比均已实现，默认入口已切换；真实 API 已接入导入导出、详情编辑、Tag、批量操作、素材版本、文档自动保存、分组、重复项、规则与快速整理。
- 本轮补齐主窗口右键与拖入/拖出接线、当前视图全选、Tag 重命名/删除、启动配置/重试/索引补齐、工具箱选区广播与定位回图库、换库清理、导航及分组分页恢复。自动规则确认使用统一弹窗；详情原图支持缩放、平移和重试。
- 默认运行方式：`npm run dev`，开发模拟页 `http://127.0.0.1:1422/?mock=1`；工具箱追加 `&window=toolbox`，对比追加 `&window=compare&row=1`。`npm run build` 与 Tauri 默认构建使用 React。独立 `dev:react` 预览入口仍保留；本机程序已按用户要求替换为 React 版本（构建代码 `0acb7c6`）。
- 当前验证：React 类型检查和生产构建通过；现有 38 项单元测试通过。内存回归通过启动错误重试、64 行分组/重复项全选、6 万行折叠分组全选、过期选区取消、视图/排序导航，以及恢复 400 个分页成员、320 个显示上限和滚动位置。
- 此前浏览器回归通过：大库虚拟化与搜索竞态、过滤及数值校验、详情四字段和撤销、Tag 三态批量编辑、JSON 取消与失败重试、素材多版本和图库选图、提示词文档自动保存和失败重试。工具箱八个常规工具的 900×600 布局检查通过。上述结果不能代替本轮新增接线的最终复验。
- 收尾范围：用户于 2026-09-21 明确要求“不用测了，写完了的话就结束吧”，因此停止后续测试并完成默认 React 入口切换。最后的入口调整已在本机安装构建中通过类型检查与打包，未补做功能测试；新增控件最小窗口布局、自动规则确认、原生文件对话框/拖放/跨窗口/导出不宣称已验收。旧 Svelte 文件及依赖保留供回溯，不再由默认入口加载。未新增测试文件；本机安装仅替换程序，未主动改动用户资料库。
- 代码组织重构已完成：工作分支 `refactor/code-organization`；重构前基线 `6dc941a`、备份标签 `backup/pre-structure-refactor-20260920` 已推送。
- 命令、运行时、规则引擎、快速整理和图片导入按职责组织；前端全局状态拆分，五个大型功能页面的业务流程迁入控制器，共用筛选快照、文本处理、滚动恢复、定位和编辑能力。
- 入口说明见 [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md)，范围、复用清单和验证记录见 [docs/REFACTORING.md](docs/REFACTORING.md)。
- 已按用户要求将代码 `0acb7c6` 构建为 React 本机正式运行版本并安装；未发布 Release，版本号保持 `0.12.14`。未跟踪的 `.claude/` 保持原样。

## 上一阶段结构重构验证（旧版）

- 沿用现有测试：前端 38 通过；Rust 301 通过、3 项原有性能测试忽略；未新增用例。
- 类型检查与生产构建通过，静态前端依赖检查未发现循环；保留 1 条缩略图可访问性警告及原有包体积提示。
- 本地安装：Tauri release 编译通过，安装文件 SHA-256 与构建产物一致，新进程已启动并响应；未运行安装包发布流程。
- 隔离浏览器验证通过：浏览 / 搜索 / 历史与滚动恢复、详情编辑、素材保存失败重试与版本复制、文档自动保存、规则保存预览、快速整理应用 / 撤销 / 重做、导出选项及请求。900×600 主窗口无横向溢出。
- 本次未重新实测：Tauri 原生对话框、资源管理器拖入 / 拖出、真实跨窗口消息、安装包覆盖安装、智绘姬 JSON 实际加载。

## 待发布与原有待办

- 最近发布 `v_40`，应用版本 `0.12.14`；发布流程见 [docs/RELEASING.md](docs/RELEASING.md)。
- 待发布：随机画师串稳定与动效、XML artist 块解析及历史数据修复、主窗口浏览历史、素材分区及多版本功能、此次结构重构，以及 React 滚动与筛选闪烁修复。
- 素材包括元数据选取、图库选图、共享画廊布局 / 筛选 / 详情 / 删除确认、按需滚动、多版本编辑 / 默认复制 / 指针排序；数据库 schema v20。多版本外观、拖动排序和实体鼠标侧键仍保留原有实机验收事项。
- 2026-09-21 本机安装路径：`D:\应用\智能表格\smart-spreadsheet.exe`；现为 React 主窗口、工具箱和对比版本，版本号 `0.12.14`。SHA-256：`4499036DC65B2357E8B7C5670B3168D96035D30F54541B746D772F525C62D25C`。
- 本次替换前的 Svelte 程序已备份并校验：`D:\应用\智能表格\backups\20260921-014600-before-react\smart-spreadsheet.exe`。安装文件与 `target/release/smart-spreadsheet.exe` 的 SHA-256 一致；未启动应用或继续功能测试。
- 后续可单独核查：后台任务互斥、筛选缓存失效、跨窗口编辑通知与选择范围一致性；本次未混入行为修复。

## 维护约定

只保留当前交接信息，不追加开发流水。历史查 Git；发布后移除已发布摘要。
