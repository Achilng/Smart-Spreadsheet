# 开发进度

最后更新：2026-07-26

## 当前状态

- 完成 UX 交互重构四个阶段（分支 `agent/minimal-ui`，与 UI 视觉重构并行推进）：
  - **阶段1 数据安全**：模态浮层打开时全局 Delete/Ctrl+A/Ctrl+Z 短路（modal-layer
    模态栈）；右键未选中项时选区收缩为该项（资源管理器惯例）；全选统计的
    await 前后版本校验消除竞态；排序切换重置 Shift 锚点；删除分组走确认对话框
    （文案明确图片不删、回到未分组、可撤销）；快速整理执行前按张数确认；自动
    规则支持未保存草稿直接只读测试（新增 preview_automation_rule_draft 命令），
    条件/任务切换类型与删除前确认；详情面板编辑器脏状态守卫（Esc 确认、切行
    草稿暂存可恢复）；提示词文档保存失败后切换文档需确认；两窗口 onCloseRequested
    关闭守卫（任务进行中/未保存草稿/撤回栈非空时拦截确认）。
  - **阶段2 基础组件**：Modal 加 Tab 焦点陷阱、关闭归还焦点、busy 时 Esc/遮罩
    不关闭、labelledby 透传；右键菜单按视口夹取（底部/右缘自动翻转）+ 方向键/
    Home/End 键盘导航 + 打开聚焦首项；通知系统队列化（最多 3 条右下角堆叠、
    error 常驻手动关、进度与通知共存、不再压住底部选择条）；分组操作静默吞错
    全部转为明确报错（"已将 0 行分配"假成功、重命名无声失败等）；删除 Tag 确认
    显示影响行数 + busy 态防并发；拖入不支持类型给明确提示；更新现有图片预告
    清空撤销栈。
  - **阶段3 长任务**：规则测试/执行、快速整理预览/应用、画师前缀、数据迁移、
    重置全部改 async + spawn_blocking，不再卡死窗口；parallel_map 新增可取消
    版本，导入管线各阶段边界响应取消（写库开始后不再中断）；新增
    cancel_current_task 命令与导入进度 toast 取消按钮；工具箱三大工具并入
    app.busy 互斥防止并发写库。
  - **阶段4 交互惯例**：Ctrl+单击图片加选/取消、Shift+单击范围选（单击仍是看
    详情）；Shift 跨未加载页时明确提示实际选中数；缩略图"取消≠失败"（滚出视口
    不再永久显示"不可用"，真失败可点击重试）；外拖后死点击修复；空状态按实际
    筛选维度生成文案 + 一键清除；两窗口输入框放行原生右键菜单（粘贴可用）；
    工具箱窗口聚焦/主窗口广播时刷新快照（工具可用性不再冻结）；导出工具改为
    无库可用；首屏支持拖放导入；重复项/分组卡片显示文件名 + 分辨率。
  - 验证：Rust 全量测试 229 项通过（新增草稿预览与取消测试）、Clippy 严格、
    前端检查 0 错误、生产构建通过；Playwright 模拟 Tauri IPC 冒烟测试
    阶段1（9 项）/阶段2（7 项）/阶段4（7 项）全部通过。
- 前端完成第三轮结构性重构，主窗口完整对齐"画纸"设计稿（分支 `agent/minimal-ui`）：
  52px 统一标题栏（品牌区 + 分段控件视图切换替代左侧 NavRail + 搜索胶囊 +
  文字风按钮）、画布头部（24px 大标题 + 匹配计数 + 排序/尺寸控件迁入，各视图
  旧工具条删除）、筛选 chips 行（搜索词/Tag/开关逐项可删 + 一键清除，与顶栏
  搜索框双向联动）、侧栏筛选化（check-row 排版 + 生效计数 + AND/OR 文字切换，
  筛选/打标双模式保留且模式提升至全局状态）、底部通栏选择条（打 Tag 联动侧栏、
  导出所选上弹菜单、取消选择蓝字）、详情面板文件名标题 + 分辨率·时间副行；
  另补全局模态层栈（modal-layer），模态打开时资料库级快捷键短路、Esc 分层关闭。
  前两轮的纸感 token（白纸面/墨黑主按钮/系统蓝/柔和大阴影/胶囊控件）与
  画册式卡片全部保留。
- 工具箱窗口完成"画纸"语言精修（7 个 commit）：全局层新增 select/radio 接管
  （appearance:none + 自绘箭头/墨黑圆点）、自绘 .progress 进度条、currentColor
  .spinner、.tool-card/.step-badge/.metric-grid/.action-bar/.sample-grid/.txt-*
  共性组件类；外壳 52px 标题栏 + 24px 画布标题 + 侧栏灰选中态 + 自动规则页
  fullBleed 单头；九个大页面逐页收编全局类，清零硬编码警示色（琥珀/绿系换
  --warning/--success token）、文本字符图标换 Lucide、三份 spinner keyframes
  与四份指标宫格实现合一；维护页补上感知哈希进度条渲染。
- 详情面板"生成信息"kv 表已上线（模型/采样器/步数 CFG/种子，Rust 暴露五字段）。
- 工具箱快速整理的画师前缀操作已支持跨逗号的 NovelAI 数值权重区段；例如输入
  `rourow` 可将 `1::artist:huangdanlan, rourow::,` 正确改为
  `1::artist:huangdanlan, artist:rourow::,`，同时保留结束符与原始空格。
- 资料库排序按钮补回单一下拉箭头，并在菜单展开时旋转，明确提示该按钮可选择。
- 资料库排序控件已进一步精简：移除顶部浏览说明、菜单项装饰图标和按钮内的
  “排序方式”副标题，按钮仅显示当前排序名称。

- 主程序当前可用。
- 当前数据库 schema 为 v14；新库不再依赖 Excel，XLSX 仅作为导出格式。
- 本次发布版本为 0.10.0（GitHub Release `v_24`）。

## 已交付能力

### 图片资料库

- 支持从单张 PNG、文件夹、ZIP、7z 和 RAR 导入 NovelAI 图片。
- 解析 PNG 文本块及 NovelAI Alpha 通道隐写中的正向提示词、角色提示词、
  负向提示词、画师串、VIBE 引用和完整元数据。
- 通过来源身份、SHA-256 和元数据指纹识别重复内容，并可重新关联搬家或重新保存
  后的原图。
- 使用 SQLite 保存图片记录、Tag、分组、备注和导入信息。

### 浏览与整理

- 提供虚拟化画廊和表格，支持搜索、Tag AND/OR 筛选、分组、批量选择和批量编辑。
- 画廊和表格顶部提供共用排序选择，支持时间正序、时间倒序与最近更新；新导入、
  提示词编辑、备注、Tag 增删及分组变更会进入最近更新时间排序。
- Tag 库可勾选“筛选存在 VIBE 的图片”；该条件覆盖分页、全选、批量打标、重复项
  浏览和当前结果导出。旧资料库首次打开时会自动建立 VIBE 数量索引。
- 支持提示词、备注、Tag 和分组操作的会话级撤销/重做；图片删除和更新现有图片
  等高风险操作不进入撤销历史。
- 画廊依次使用 256px 缩略图、1024px 高清图和 2048px 详情预览，全屏查看时
  按需读取原图。
- 画廊卡片显示 VIBE 数量；详情支持提示词、备注、Tag、图片信息和原图预览。

### 工具箱

- 独立工具箱窗口承载自动规则、随机画师串、以图搜图、智绘姬 JSON 去重、感知哈希维护、
  失败图片目录、数据管理、图片导出、全库快速整理和画师前缀修正。
- 自动规则是与快速整理分开的持久化功能，可在没有资料库时预先编写。规则支持启停、
  排序、导入/更新触发开关、两层 AND/OR 条件组及整体“条件成立/不成立”；条件覆盖
  提示词、Tag、分组、画师、备注、文件信息、VIBE、元数据、尺寸构图和完整 NovelAI
  生成参数。任务支持 Tag、分组、提示词、画师前缀、备注及按图片停止后续规则。
  正式新库不包含任何预置规则；角色固定形态与 Tag 的示例只存在于回归测试中。
- 自动导入规则只处理本次真正新增的行，重复图片不会触发；更新规则只处理本次成功
  更新的既有行。每条规则使用独立事务，单条配置或执行失败会在导入结果中报告，
  不回滚已导入图片，也不阻断后续规则。现有资料库可先只读预览命中/改动数和示例，
  再确认手动应用；产生修改时清空旧撤销/重做栈，避免历史操作覆盖规则结果。
- 画师前缀修正从 Danbooru 分页同步所有画师 Tag（包括零发帖 Tag）、现存 Artist
  记录中的其它名称，以及有效/退役画师别名。受 takedown 限制但仍存在的画师记录
  会保留识别，已删除的旧 Artist 记录不会单独建立身份；历史名称仍为画师 Tag、别名
  或现存记录其它名称时继续可用。因此 `parsley-f`（现存、受限）与 `parsley_f`
  （历史写法）会归并为同一身份并正确补前缀。
- 自动扫描正向、角色和负向提示词中的完整裸 Tag，保留 NovelAI 权重与括号，跳过
  已有 `artist:` 前缀和相似名称。候选支持搜索、逐项选择及在主窗口查看示例；默认只
  排除需确认项。发帖量低于 20 会显示“低使用量”并默认不勾选；极短名称、常见英文词
  或身份冲突同样默认要求人工确认。应用后重算正向/角色来源的画师串，支持
  会话级撤回与重做，且不读取或改写原始图片文件。
- 扫描会先收集全库正向、角色和负向提示词中已经明确使用 `artist:名称` 的 Tag，
  将它们作为“库内已有明确标注”的最高优先级证据。其它图片中完全匹配的同名裸 Tag
  会默认勾选，即使该名称不在 Danbooru 或不足 20 帖；仅名称相似的内容仍然跳过。
- 安装包已内置 2026-07-22 的压缩画师词典快照：包含 719,369 个画师 Tag、
  526,957 个带其它名称的现存 Artist 记录、32,372 个别名，归并后得到
  1,577,313 个可识别名称。资源约 27.9 MB，首次打开工具时流式写入当前资料库，
  实测约 6.44 秒；后续可使用原有“更新画师词典”按钮联网刷新。
- 快速整理支持按正向提示词、角色提示词、负向提示词、画师串与备注中的严格
  AND 组合扫描整个资料库，预览后批量添加多个 Tag 或移入指定分组；Tag 可在
  工具内直接新建，临时匹配规则不入库。批量分组可勾选“仅处理未分组的图片”，
  命中但已有分组的图片会被统计并跳过，不会离开原分组。
- 快速整理“提示词操作”支持输入单个画师名，在正向、角色与负向提示词中批量
  补全 `artist:` 前缀；仅修改完整匹配的裸 Tag，保留括号及 NovelAI 权重语法，
  包括跨逗号、以 `::` 结束的数值权重区段；跳过已有前缀和相似名称，并提供影响预览、
  示例图片、撤回与重做。画师串只从
  修改后的正向和角色提示词重算，负向画师不会进入筛选用画师串。
- 提示词匹配忽略大小写和 NovelAI 常见权重语法，并将 `girl`、`1girl`、`1 girl`
  作为同一个泛用标签；其它内容仍严格区分内部空格、下划线及字符。执行使用
  单事务，并按实际新增关联提供精确撤回与重做。
- 点击以图搜图结果会恢复并聚焦主窗口，清除遮挡目标的筛选条件，再把对应图片
  定位到画廊中央。
- 图片导出工具实时读取主窗口选区，也可点击选择本地文件夹，或把多张图片、多个
  文件夹及其混合内容拖入指定区域；文件夹递归扫描子目录，按完整路径追加去重并
  按文件名自然排序。保留原名、随机乱码、自定义前缀序号及导出副本元数据清除等
  既有选项不变；PNG 会重新编码并清洗 NovelAI Alpha 最低位隐写载荷，输出直接
  写入用户选择的文件夹并自动避让同名文件，不修改原图或资料库。
- 分组管理保留在分组视图内，不放入通用工具箱。

### 导出与维护

- 支持将当前筛选或选中结果导出为带缩略图的 XLSX、智绘姬 JSON 或图片文件包。
- 工具箱可将主窗口明确选中的图片导出到指定位置，并按需重命名或重新编码清除
  导出副本的元数据。
- 支持数据目录迁移、资料库重置、感知哈希刷新和失败图片检查。
- 更新现有图片只更新已匹配记录，不新增图片，并保留用户的 Tag、分组和备注。

## 最近有效变更

- 自动规则的列表型输入新增全角逗号防呆：Tag、提示词组合和画师名现在统一接受
  半角逗号 `,`、全角逗号 `，` 或换行；界面明确标注支持范围，并对 Tag、画师列表
  即时拆分，后端在校验、匹配、执行和自动创建 Tag 时再次解析，旧数据或直接接口调用
  也不会生成带全角逗号的组合 Tag。顿号 `、` 不作为分隔符，仍可用于名称本身。
  新增端到端数据库回归用例。
- 新增通用自动规则引擎和独立规则编辑器：数据库升级至 schema v14，持久化规则顺序、
  触发时机、两层条件组和任务列表，并缓存规则所需图片尺寸及 NovelAI 生成参数。
  导入仅对新增行执行，更新仅对成功更新行执行；规则逐条事务隔离并输出执行报告。
  工具箱支持新建、编辑、启停、排序、删除、只读预览和确认应用，首次导入页面可直接
  打开规则编辑器。用户提供的“固定形态 → 添加 Tag”只作为动态创建的集成测试，
  源码、迁移和新库均无硬编码预设。Rust 全量测试 225 项通过、1 项性能基准忽略，
  Clippy `-D warnings`、前端检查、生产构建及隔离目录 Tauri debug `--no-bundle` 构建通过；
  Playwright 完成组合条件、多任务、保存、预览、确认应用及 900px 最小宽度验证。
- 修复快速整理画师前缀操作无法识别跨逗号数值权重区段末尾 Tag 的问题：按照 NovelAI
  `数字::内容::` 语法，将末尾无数字的 `::` 作为权重结束符而非画师名的一部分；支持
  `rourow::` 与官方示例风格的 `rourow ::`，修改时保留原始空格和结束符，并同步重算
  画师串。新增快速整理完整预览/应用回归测试及底层手动、自动前缀解析测试。Rust 全量
  测试 213 项通过、1 项性能基准忽略，Clippy 严格检查通过。
- 增加库内画师证据检测：从带权重、圆/方/花括号的显式 `artist:` Tag 中提取规范化
  名称，再匹配全库其它提示词中的同名裸 Tag。库内证据可确认 Danbooru 未收录名称，
  并覆盖低发帖量、短名称等默认确认限制；界面单独标记证据来源并优先排列。新增未知
  画师、正向/角色/负向传播、相似名称跳过以及低发帖量覆盖测试。Rust 全量测试
  211 项通过、1 项性能基准忽略，Clippy 严格检查和前端检查通过。
- 根据实际候选噪声调整自动选择规则：Danbooru 发帖量低于 20 的画师名称继续显示，
  但统一进入人工确认并默认不勾选；20 帖及以上不因发帖量单独降级。常见提示词规则
  本轮保持不变，并新增 19/20 帖边界回归测试。
- 将画师词典改为随安装包提供：增加可续传的 Danbooru 快照生成器、离线身份归并工具、
  27.9 MB 流式压缩资源和完整资源验证工具。首次打开不再联网下载约 1,280 个 API 分页；
  已有或联网更新后的词典不会被内置快照覆盖。全库扫描与应用也改为只查询提示词中实际
  出现的名称，避免加载 157 万条完整词典造成内存峰值。Rust 全量测试 208 项通过、
  1 项性能基准忽略，Clippy 严格检查、前端生产构建及 Tauri debug `--no-bundle`
  构建通过。
- 在工具箱新增“画师前缀修正”：增加 Danbooru 画师词典的联网分页同步、本地原子
  替换和 schema v13 持久化；合并现存 Artist 其它名称、有效/退役别名、零发帖及
  受限画师 Tag，并处理多级历史别名链。新增资料库一次扫描、风险分类、候选预览、
  批量补全和撤回/重做；覆盖 `parsley-f / parsley_f`、短常用词、NovelAI 权重、
  正向/角色/负向同步修改及历史名称归并测试。Rust 全量测试 206 项通过、1 项性能
  基准忽略，Clippy 严格检查通过；前端检查 0 错误，生产构建通过（保留 3 条既有
  图片拖动可访问性警告）。
- 重做画廊与表格的排序菜单：移除系统原生下拉框，改为带当前模式图标、简短说明、
  清晰选中态的自定义弹出菜单；加入弹出缩放淡入、箭头旋转、图标与选项悬停反馈，
  同时支持点击外部关闭、Esc、方向键/Home/End 导航和减少动态效果偏好。窄窗口下
  触发按钮会收进视图自身宽度，不再覆盖侧边栏。Playwright 在 1280×800 和
  760×520 下验证画廊、表格、模式切换、菜单关闭与响应式布局。
- 为画廊和表格增加时间正序、时间倒序、最近更新三种共享排序。数据库升级至
  schema v12：新行以导入时刻初始化更新时间，历史行用原批次导入时间回填，
  提示词、备注、Tag 与分组的实际变更由触发器自动更新时间，内部哈希和图片状态
  维护不会扰乱顺序；单行编辑在最近更新模式下会立即刷新分页并将目标移到前面。
  “最常用”按本轮决定暂不实现。新增排序、定位、编辑触发和 v8/v9 升级回归测试；
  前端检查与生产构建通过，Rust 全量测试 195 项通过、1 项性能基准忽略，Clippy
  严格检查通过。
- 为工具箱图片导出增加本地批量选图入口：点击拖放区打开系统文件夹选择器；拖放
  支持单张/多张图片、单个/多个文件夹及混合输入。后端递归扫描所有子文件夹，
  支持 PNG、JPG/JPEG、WebP、GIF、BMP、TIF/TIFF，按规范化完整路径追加去重，
  再按文件名自然排序；新增来源与主窗口选区统一进入原有导出管线，重命名、重名
  避让、并发导出和元数据清除行为保持不变。界面显示追加数量、少量文件名预览，
  并允许单项移除或一键清空。新增递归扫描、自然排序、重复路径以及空主窗口选区
  导出外部图片的回归测试；前端检查与生产构建通过，Rust 全量测试 193 项通过、
  1 项性能基准忽略，Clippy 严格检查通过。
- 修复“筛选单画师串图片”偶尔混入多画师图片：快速画师前缀修正统一将重算结果
  保存为逐行画师串；资料库首次打开会自动修复历史的逗号分隔记录，无需重新导入
  原图；筛选查询也会直接排除尚未修复的逗号多画师串。新增旧库自动修复及新旧格式
  筛选回归测试，全量 Rust 测试 191 项通过、1 项性能基准忽略，Clippy 严格检查通过。
- 引入开源 `@lucide/svelte` 图标库，将主导航与尺寸滑块的手绘 SVG 迁移为
  按图标入口加载的组件；参考“花笺”的克制线性风格，主导航统一为 18px，
  工具箱入口为 15px，窗口控制为 13px，并改用更直接的资料、列表、文件夹、
  搜索和设置语义图标；最小化、最大化/还原和关闭按钮也统一使用 Lucide，
  仅应用品牌图标继续保留专用 SVG。工具箱七个工具入口同步从字符符号迁移
  为语义化 Lucide 图标。前端检查和生产构建通过，Playwright 模拟 Tauri
  运行时后完成主窗口与工具箱视觉核对。
- 稳定 Windows MSVC 开发构建：仅将应用 crate 的 Debug codegen 单元收敛为 1，
  保留增量编译和依赖并行构建，避免 `tauri dev` 链接 cdylib 时出现 LLVM 匿名
  符号 `LNK2019`；全量测试后再次执行普通开发编译已稳定通过。
- 为快速整理的批量分组增加“仅处理未分组的图片”选项；预览单独显示跳过的
  已分组图片，执行、通知与结果均报告实际加入及跳过数量，撤回只恢复实际改动。
- 为快速整理条件增加首组泛用标签别名：`girl`、`1girl`、`1 girl` 双向等价，
  同时保持 `2girls`、`girlfriend` 等相似标签不命中。
- 统一快速整理目标列表的选中样式：分组仍为单选，但改用与 Tag 相同的方形
  `✓` 勾选反馈，移除视觉不一致的圆点样式。
- 优化快速整理的 Tag 与分组新建入口：各自搜索框右侧改为按需展开的“＋ 新建”
  按钮，点击后显示名称输入框及创建/取消操作，创建成功后自动收起并选中新目标。
- 开放快速整理“提示词操作”，完成全库画师前缀修正：一次输入一个画师名，
  同时处理正向、角色、负向提示词，并按图片及提示词字段统计影响范围；执行和
  撤回均使用单事务，精确保存三类提示词和画师串的修改前后状态。
- 完整退役电子表格导入及旧工作簿运行时依赖，保留生成式 XLSX 导出。
- 工具类功能迁入独立工具箱窗口，主窗口聚焦资料库浏览、导入和导出。
- 完成以图搜图结果到主窗口画廊的跨窗口定位。
- 将“快速编辑”更名为“快速整理”，新增工具内新建 Tag 和“提示词组合 →
  指定分组”动作；分组执行会逐张记录原分组，撤回恢复各自原状态，重做再次
  移入目标分组，并继续为提示词替换、追加和删除动作保留复用结构。
- 完成工具箱图片导出：同步主窗口选区，支持保留原名、随机名、自定义序号命名
  和仅对导出副本重新编码清除元数据。
- 修正 PNG 导出副本的元数据抹除：除文本、EXIF、ICC、时间、物理尺寸、颜色
  建议和私有附加块外，同时清除 NovelAI 写入 Alpha 通道最低位的
  `stealth_pngcomp` 隐写载荷；移除导出时逐张二次解码复检，改由自动化测试验证
  普通块与隐写层均已清除，缩短批量导出耗时。
- 优化大批量图片导出：改为并行流水线，按机器逻辑核心数与本批最大图片的估算
  解码内存共同限流（最多 8 个编码任务，纯复制最多 4 个），PNG 使用快速压缩和
  `Sub` 滤镜；开发版启用最低优化级别，避免 Debug 编解码慢几十倍。真实 56 张、
  约 2.70 亿像素样本由约 160.7 秒降至 2.218 秒，内置检测器复检 56 张均无元数据。
- 表格导入检测器同步支持 Alpha 隐写元数据，避免把只有隐写载荷的新式 NovelAI
  图片误判为无元数据。
- 验证：Rust workspace 库测试 193 项通过、1 项性能基准忽略；Clippy
  `-D warnings`、前端检查和生产构建通过；Playwright 在 960×680 与
  760×520 下完成快速整理 Tag 新建、批量分组预览及无横向溢出，并检查导出
  工具选区同步、重命名和元数据开关。
- 完成画廊渐进式高清加载、分层缓存和 VIBE 徽章。
- 完成 VIBE 图片筛选及 schema v11 数量索引；新导入和更新现有图片实时写入，
  v8–v10 资料库自动升级并回填历史图片。
- 修复分组与重复项视图的卡片未显示 VIBE 数量徽章；两种视图现与画廊共用
  同一 VIBE 状态缓存并按卡片加载引用数量。
- 完成原图内容哈希 / 元数据指纹重关联与受管原图可信标记。

## 当前待办

1. 重新确定正式界面的视觉与信息架构方向。
2. 补充资料库备份、恢复和数据库健康检查入口。
3. 改善原图重关联出现多个候选时的人工选择流程。
4. 继续优化大资料库筛选、图片缓存淘汰和批量操作性能。

## 验证基线

- 前端：`npm run check`、`npm run build`。
- Rust：workspace 库测试、Clippy `-D warnings`、Tauri debug `--no-bundle` 构建。
- Native 文件对话框、安装包覆盖安装和智绘姬 JSON 实际加载仍需发布前人工抽查。

## 已知约束

- v8–v13 数据库可自动升级到 v14；v1–v7 需要先使用旧版本升级。
- XLSX 只允许生成式导出，不读取、修改或依赖旧工作簿。
- 不安装全局 `cargo-tauri`，使用仓库内 `@tauri-apps/cli`。
- Node 版本保持不变。
