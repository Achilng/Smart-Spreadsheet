# 当前进度

最后更新：2026-09-20

> 只保留当前交接信息，目标不超过 40 行。更新时替换旧状态，不追加开发流水；已完成事项在发布后移出，历史查 Git 和版本更新说明。

## 当前状态

- 已移除素材侧栏的 Tag 搜索框及对应过滤逻辑；侧栏直接展示 Tag 列表，顶部素材搜索与 Tag 多选筛选保留。
- 素材多版本已完成：独立名称/文本/可选图片，共享 Tag、固定封面；双击始终复制首项。版本入口使用图片角落的无边框图层图标与数量，局部渐变略加深；与图片共用悬停/按压变换、时长及图片中心缩放原点，菜单保持视口定位。复制后菜单保持展开，鼠标移出入口及菜单后收起，跨越间隙有 180ms 宽限。详情按画廊分节纵向展开全部版本，每节独立编辑与复制。
- 版本拖动改为指针捕获，避免依赖 WebView 原生拖放；支持拖动浮层、插入提示、边缘自动滚动、Esc/失焦取消，放开后保存草稿顺序并平滑归位；保留上下移动和设为默认。详情图片按可见区域加载，文本自然展开。
- 素材已取消手动翻页，复用画廊可见范围计算连续滚动并按需取数；跨区保留滚动位置，筛选后回到顶部，过时请求失效，后续加载失败可原地重试。左侧共用 TagFilterSidebar，搜索迁至 TopBar，素材与画廊各自保存搜索词；素材卡片下方名称居中。
- 素材右侧详情已复用画廊的 DetailSidebar、DetailPanelLayout、DetailPreview、DetailLightbox 与分节样式：固定标题/操作、Tag 和文本分节、点击放大、收起/展开及同款动画；编辑仍进入素材编辑窗口，复制按钮显示短暂成功反馈。
- 「素材」分区已完成、待发布：图片卡片、每版本单段文本、共享 Tag、单击详情/编辑、双击复制，支持图库选图、本地文件选择与拖入后逐张确认、PNG 元数据多区域选择。
- 素材与版本图片随数据库保存，schema 升至 v20；旧素材自动保留为一个默认版本；版本、图片、顺序和 Tag 在同一事务保存，搜索覆盖所有版本名称和文本。
- 素材已直接复用画廊：两处共用 GalleryTile、GalleryViewport、网格计算、Thumbnail、CardTagSummary 和 SizeSlider；统一方形图片、下方名称、图片内 Tag、选中反馈和尺寸设置。已移除素材独立卡片样式，保留详情编辑与双击复制；素材图片加载使用独立缓存，不触发画廊行的文件拖出。
- 仓库记录的最近发布为 `v_40`，应用版本 `0.12.14`；发布流程见 [docs/RELEASING.md](docs/RELEASING.md)。
- `v_40` 之后已完成、待下次发布：随机画师串输出稳定与动效、XML `<artist>` 块解析及历史数据修复、主窗口浏览历史前进/后退。

## 验证与未结项

- 最新验证：38 项前端单元测试、类型检查及生产构建通过，新增指针拖动上下排序、取消、多指针隔离、长列表自动滚动回归；之前 301 项 Rust 测试通过，3 项手动性能测试跳过，本次未改后端。
- 本次版本拖动与纵向详情调整已热更新到运行中的开发版，实机观感及拖动由用户验收；安装版仍是下述移除 Tag 搜索框的构建。
- 多版本外观、切换动效、拖动排序和快捷复制尚未做实机验收。2026-09-20 已将移除 Tag 搜索框后的正式构建替换至 `D:\应用\智能表格\smart-spreadsheet.exe`，类型检查、生产构建及安装文件 SHA256 校验通过；未发布 Release，应用版本仍为 0.12.14。
- 最近本机旧程序备份：`D:\Agent\Agent_temp\smart-spreadsheet-local-update-20260920-163805\smart-spreadsheet.previous.exe`；替换前应用正常退出，未改动资料库。
- 用户已接手实机鼠标检查，本次停止桌面自动操作；原生文件对话框、资源管理器拖入和图库选图交互由用户验收。

- 浏览历史：22 项前端单元测试、前端检查及构建通过；模拟大库和 Windows 实机浏览验证通过。
- 待验证：实体鼠标侧键在 WebView2 中的前进/后退行为，目前只有事件模拟验证。
- 已知构建提示：1 条缩略图可访问性警告及包体积提示，尚未清理。
- 下次发布前人工抽查：原生文件对话框、安装包覆盖安装、智绘姬 JSON 实际加载。

## 维护方式

- 开工时写清当前任务、下一步和阻塞；收尾时只留结果摘要与未结项。
- 不维护完整功能清单，不重复 PLAN.md 的候选方向，不保留逐次测试数量、耗时或操作日志。
- 只保留与当前交接有关的验证结论，注明未验证范围；发布后移除已发布变更摘要。
