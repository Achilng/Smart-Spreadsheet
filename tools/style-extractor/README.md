# 外置画风提取工具

1. 在智能表格「工具箱 → LLM 画风提取」导出待处理 JSON。可选全部、选中或当前筛选范围；原文完全相同才合并，默认跳过已处理记录。
2. 双击本目录的 `启动画风提取.cmd`，或在项目根目录运行 `node tools/style-extractor/server.mjs`。
3. 浏览器打开 http://127.0.0.1:17321 ，选择 JSON 和「自定义 API」。填写 Base URL、模型名、API Key，设置每次调用条数（默认 10）和并发请求数（默认 1），创建任务后点「开始 / 继续」。
4. 完成后下载结果 JSON，在智能表格同一工具页预览并应用。相同原文的全部记录一起更新，表格和详情显示 LLM 标记，支持撤销 / 重做。

## 接口设置

需要本机已有 Node（支持内置 fetch / AbortSignal.any，例如当前 Node 24），不安装或升级环境。自定义 API 使用 OpenAI 兼容的 `/chat/completions`；Base URL 填到 `/v1`，无需附加 `/chat/completions`。

本次默认接口 `https://oneapi.hakoyu.com/v1` 的模型列表实际返回 `gpt-6-luna【神秘】`，请使用完整名称，包括后缀。Base URL 与模型名均可自行修改。2026-09-24 已通过该模型名真实调用：用户原始 3 个示例逐字一致，另一次单批 10 条（原始 3 条加 7 条小样本）全部通过原文校验并与预期一致，用时约 11 秒。小样本不代表整个资料库的识别准确率，也不验证第三方底层模型身份。

API Key 按 Base URL 分别保存到 `data/.credentials.json`，内容由 Windows DPAPI 的当前用户模式加密，仅同一 Windows 账户可解密；不写入任务 JSON、日志、浏览器 localStorage 或 Git。填写后离开输入框会自动保存，也可以点击「保存 Key」；启动 API 任务前也会保存。重新打开网页、服务重启或切换历史任务会自动回填对应接口的 Key。「清除已保存 Key」只清除当前接口的凭据，不影响其他接口或任务结果。

当前 Key 下方会显示适用的 Base URL。切换接口后清空旧输入并读取该接口已保存的 Key；运行历史任务时按该任务实际端点匹配，不会把另一个接口的 Key 直接用于它。创建后端点和模型固定，左侧参数用于新任务；已有任务可以修改右侧并发数后开始 / 继续 / 重试。原文发送至所选端点，由该接口账户计费。本机加密保存目前仅支持 Windows。

支持 low / medium / high 推理等级。若兼容接口明确拒绝结构化输出或推理等级参数，会去掉对应参数重试，并仍严格校验返回的 JSON、编号与连续原文。

也保留「Codex CLI · 本机登录」模式，需要已安装并登录的 Codex CLI，模型为 `gpt-6-luna`。本机 ChatGPT 登录端点实测不支持此模型，任务会暂停并保留进度；不会擅自换模型。API 模式已经可以使用。

## 进度与恢复

并发数可设 1–32，默认 1；旧任务也默认 1。10 并发表示最多同时发出 10 个模型请求；若每次 10 条，则最多有 100 条同时处理。每个请求完成后独立保存，返回顺序不影响结果与原文的对应。

界面显示累计运行时间（不含暂停）、进度、批调用次数、正在进行的请求数、无画风和失败数量。每批完成保存到本目录 `data/`，请保留它；关闭网页不停止任务。输入与进度分开保存，避免频繁重写整份正文。服务只监听 127.0.0.1。

点击暂停会取消所有正在进行的调用，等待它们退出后才能继续；各批未保存的条目在继续时重新请求。关闭服务或电脑后重新启动，选择历史任务继续；成功项不重跑。网页短暂断线会自动重连，服务重启后网页自动刷新恢复任务列表。每批数量为上限，长正文批次会自动缩小，单条不截断。

临时失败最多尝试三次；登录、权限、额度或限流错误会取消其余请求并暂停整个任务，保留已保存结果，解决后继续。失败项可单独重试，也可下载已完成部分；失败 / 未处理项导入时保持表格旧值。成功的「无画风」结果会清空旧值并保留 LLM 标记。修改正向正文后标记失效，角色 / 负向 / 备注的修改不影响它。提示词变更后旧任务禁止继续，以免混用规则；仍可下载旧结果或创建新任务。

输入和导入文件上限均为 64 MB。可选环境变量：`STYLE_PORT`（默认 17321）、`STYLE_DATA_DIR`（任务保存目录）、`STYLE_TEMP_DIR`（CLI 临时目录，默认 D:/Agent/Agent_temp/style-extractor）、`STYLE_CODEX_BIN`（Codex 可执行文件路径）。测试临时目录与正式 data 分开。

验证：`node --test tools/style-extractor/core.test.mjs`。
