# 项目计划

## 项目定位

智能表格是面向 NovelAI 图片的本地资料库，核心流程是：导入图片 → 检索与整理 → 对比与导出。

- 技术栈：Tauri 2、React 19、TypeScript、Rust、SQLite。
- 前端方向：已确定迁移到 React，配合 shadcn/ui 精修细节；保留当前结构、功能位置和白色配色。React 三个窗口已实现，默认开发与生产入口已切换；用户要求停止后续测试并收尾，范围与已验证边界见 [React 重写计划](docs/REACT-REWRITE.md)。
- 输入：PNG、文件夹和 ZIP / 7Z / RAR 压缩包，解析图片中的生成元数据。
- 整理：画廊、表格、分组、Tag、筛选、重复项、提示词编辑和图片对比。
- 素材：独立图片卡片库，固定封面与共享 Tag；同一素材可保存多个命名版本，每个版本一段文本和可选图片。卡片版本列表点击即切换并复制对应文本，也可双击卡面复制当前版本，导入时确认并选择 PNG 元数据区域。
- 输出：带缩略图的 XLSX、智绘姬 JSON、轮询脚本 JSON 和图片文件。
- 独立工具箱承载批量处理、自动规则和数据维护，主窗口聚焦浏览与整理。

工具箱提供画风提取工作台与批改台入口，点击后启动本地服务并在默认浏览器打开。开发者可通过 `tools/style-review` 独立批改提取结果，手工检查边界并统计已批改样本正确率。模型代理可独立部署到服务器，通过现有自定义 API 模式接入。

## 后续方向

已实现外置 LLM 画风提取（数据库 v21、自定义 API / Codex CLI），协议、来源与状态约定见 [专项设计](docs/LLM-STYLE-EXTRACTION.md)。

以下是候选事项，不代表已经排期。开始任务时明确范围和验收方式；当前正在做什么只写在 [PROGRESS.md](PROGRESS.md)。

| 方向 | 要解决的问题 | 完成标准 |
| --- | --- | --- |
| 备份、恢复与健康检查 | 用户缺少直接检查和恢复资料库的入口 | 能备份、验证并恢复资料库，明确报告异常和结果 |
| 原图重关联冲突处理 | 原图搬家后出现多个候选，只能拒绝自动匹配 | 展示候选差异，由用户选择并验证完整原件 |
| 大资料库性能 | 筛选、图片加载和批量操作可能随规模变慢 | 用可复现样本定位瓶颈，记录修改前后耗时与内存变化 |
| React 前端重写 | 保留当前结构与白色配色，精修控件和交互细节，迁移现有全部前端功能 | 先交付同结构可交互样稿，再分阶段迁移三个窗口；功能、视觉和性能均通过专项计划验收 |

## 长期边界

- **排版易读性**：参考 Apple Typography 的字号、字重与对比度原则，辅助文字以 13px 为下限、常用控件 14px、正文 15px、标题 17–24px；用常规与半粗体建立层级，辅助灰字在白色和浅灰背景上保持至少 4.5:1 对比度。字号统一使用共享变量，调整卡片文字区时同步虚拟列表几何尺寸。

- **界面字体统一**：主窗口、工具箱和对比窗口统一使用微软雅黑（Microsoft YaHei），数字、输入控件及文本内容遵循同一字体设置；素材卡片名称保留华文宋体（STSong），以宋体（SimSun）回退。

- **本地数据优先**：核心整理功能在本地完成；应用更新只在用户手动检查后联网，下载和安装需明确确认。
- **保护原件**：提示词、Tag、备注等编辑写入资料库；清除元数据和重编码只处理导出副本，不覆盖原图。
- **原件身份可信**：重关联依赖内容哈希或完整元数据指纹，多候选不自动猜测；拖出图片必须取得完整原件，不能用缩略图代替。
- **操作范围一致**：分页、全选、批量修改和导出使用相同的筛选结果集，避免处理不可见的图片。
- **批量修改可恢复**：沿用已有预览、撤销与重做机制；自动规则按规则隔离失败，不回滚已完成的导入。新库不预置业务规则。
- **表格只作导出**：XLSX 全新生成，不恢复电子表格导入或旧工作簿运行时依赖。
- **兼容范围明确**：当前数据库结构为 v20，v8–v19 自动升级；v1–v7 需先用旧版升级。不主动删除数据目录中的未知历史文件。
- **素材随库保存**：文本、展示图与缩略图保存在数据库中，随资料库备份/迁移；展示图最长边 1600 像素，原始图片不受修改。
- **素材选图来源**：库内选图直接使用现有画廊，选完返回保留的素材草稿；支持本地文件导入，共用预览、元数据区域选择和保存确认流程。
- **素材浏览**：保留白色页面、Tag 筛选、顶部搜索、尺寸调节及右侧详情；素材卡片独立采用 832:1216 竖图、底部叠字、悬停浮起与跟随光效，复用画廊的列定位和虚拟滚动计算，按可见区域取数。搜索状态独立于画廊。
- **素材版本**：首项作为默认版本，详情顶部复制仍复制首项；卡片右键展开侧边版本目录，卡片下方不显示版本名或切换按钮，卡面右下角以叠卡图标和数字显示版本总数，同一时间只展开一张，在原占位内缩小并左移，不推动相邻卡片。点击版本即切换图片、复制该版本文本并收起，也可双击卡面复制当前版本文本，无独立图片时使用素材封面；选择在本次会话中保留，不修改保存的版本顺序。超过 8 个版本时可搜索，支持方向键、Enter 和 Escape。编辑及删除保留在右侧详情。详情按照画廊分节纵向展开全部版本，每节独立编辑与复制。编辑排序使用指针拖动手柄、插入提示和边缘自动滚动，保留上下移动按钮。版本独立图片随对应分节显示，未设置时共用顶部固定封面；所有版本共用 Tag，旧素材升级为一个默认版本，保存失败整体回滚。
- **大库按需加载**：保留分页、分级图片缓存和过时请求淘汰，避免全量渲染或在界面线程执行重任务。

## 文档与实现入口

| 内容 | 位置 |
| --- | --- |
| 当前进度、阻塞与待验证项 | [PROGRESS.md](PROGRESS.md) |
| 发布步骤与更新签名 | [docs/RELEASING.md](docs/RELEASING.md) |
| 代码组织重构、复用边界与回档 | [docs/REFACTORING.md](docs/REFACTORING.md) |
| 各版本面向用户的更新说明 | [.github/release-notes/](.github/release-notes/) |
| 图片对比的专项设计 | [对比功能实现计划.md](对比功能实现计划.md) |
| 自动规则 AI 助手提示词 | [AUTOMATION_RULE_AI_PROMPT.md](AUTOMATION_RULE_AI_PROMPT.md) |
| 数据库结构与迁移 | [src-tauri/src/db/](src-tauri/src/db/) |
| 前端交互与后端业务 | [src/lib/](src/lib/)、[src-tauri/src/](src-tauri/src/) |

具体功能行为以代码和测试为准；历史实现过程查 Git，不在本文件复述。

## 维护规则

- 本文件只维护定位、候选方向、长期边界和入口，目标不超过 100 行。
- 完成事项从候选列表移除，不追加“已完成”长篇说明。
- 大功能的详细设计放专项文档，这里只留链接；不逐条抄录界面细节、数据库字段或测试日志。
- 只有方向或边界发生变化时更新本文件；日常进度更新到 PROGRESS.md。

代码的职责划分与修改入口见 [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md)。
