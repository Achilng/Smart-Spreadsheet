# 智能表格 - NovelAI 图片表格 Tag 管理工具

## 1. 项目目标

开发一个 Windows 桌面工具，解决 Excel 不便进行交互式 Tag 管理的问题。第一版定位为固定 NovelAI 表格结构的“增强版 Excel”，重点提供：

1. 导入并浏览图片、时间、提示词、画师等元数据。
2. 为单行或多行添加、删除自定义 Tag。
3. 使用全局 AND / OR 模式按 Tag 筛选。
4. 在上万行数据下保持可用的加载、滚动和筛选性能。
5. 将当前结果导出为新的 Excel，在最后一列写入 Tag 字符串。

第一版不包含：任意 Excel 列映射、多文件资料库、编辑原始元数据、同步源 Excel 的后续变化、自动识图打标。

## 2. 已确认的产品规则

- 只管理一个已导入表格。导入新表格时需明确确认替换当前工作区。
- 原 Excel 永远只读，不对其写入、覆盖、重命名或移动。
- 导入后，应用将 Excel 副本及解析结果保存到自己的数据目录，不依赖原 Excel 持续存在。
- 时间、正向提示词、负向提示词、画师串、图片文件夹和图片路径均只读；只有 Tag 可编辑。
- Tag 由用户自定义并区分大小写，例如 `Landscape` 与 `landscape` 是两个 Tag。
- Tag 筛选提供全局 AND / OR 切换，不支持复杂嵌套表达式。
- 支持勾选行、全选当前筛选结果、批量添加 Tag、批量删除 Tag。
- 暂不处理源 Excel 导入后的增删、移动或重新生成。
- 导出总是生成新文件，不覆盖原 Excel 或应用保存的工作副本。

## 3. 输入表格约定

示例文件：`Examples/novelai_metadata.xlsx`

当前固定结构如下。实现时按表头识别并校验，不只依赖列号。

| 列 | 表头 | 内容 |
|---|---|---|
| A | 图片 | 与数据行对应的嵌入图片 |
| B | 时间 | 字符串时间 |
| C | 正向提示词 | 长文本 |
| D | 负向提示词 | 长文本 |
| E | 画师串 | 多行文本 |
| F | 图片文件夹 | 文件夹名称 |
| G | 图片路径 | 本地图片绝对路径 |

导入校验：

- 必须存在上述 7 个表头；缺失时停止导入并清楚提示。
- 支持文件名和路径中的中文、空格、方括号、逗号、圆括号等字符。
- 空行不导入；字段缺失的有效数据行允许导入并显示缺失状态。
- Excel 公式、样式及嵌入对象不是应用内编辑对象。

## 4. 核心工作流

### 4.1 首次启动与数据目录

- 首次启动时让用户确认应用数据目录，不在用户确认前生成大量缓存。
- 数据目录由应用统一管理，包含数据库、工作簿副本、缩略图缓存和迁移状态。
- 设置中允许“迁移数据目录”。迁移流程必须复制全部数据、校验数据库和文件完整性、切换到新目录，再清理旧数据；失败时继续使用旧目录并可重试。
- 迁移不是简单修改目录指针，也不能产生两个并行工作区。

建议目录结构：

```text
<data-dir>/
├── smart-spreadsheet.sqlite3
├── workbook/
│   └── source.xlsx
├── cache/
│   └── thumbnails/
└── migration/
```

### 4.2 导入

1. 用户通过文件选择器选择 `.xlsx`。
2. 应用校验文件和固定表头结构。
3. 将 Excel 原样复制到应用数据目录。
4. 解析行数据、嵌入图片定位信息并写入 SQLite。
5. 导入成功后原文件不再是运行依赖。
6. 任一步失败时，不替换当前工作区。

### 4.3 浏览和筛选

- 主界面采用表格式布局，展示缩略图、时间、画师、折叠的长提示词和 Tag。
- 筛选区展示已使用 Tag、当前 AND / OR 模式、匹配数和总数。
- AND：结果必须包含全部选中 Tag。
- OR：结果只需包含任意一个选中 Tag。
- 清除筛选恢复全部行。
- 图片点击后显示大图；长文本可展开、收起和复制。

### 4.4 Tag 编辑

- 单行支持添加和删除 Tag。
- 多选后支持批量添加、批量删除。
- 支持选择当前页和全选当前筛选结果，两者必须在界面上明确区分。
- Tag 保存到 SQLite，不写回工作簿副本。
- 输入时去除首尾空白，保留大小写和中间字符；空字符串不保存。
- 批量操作使用数据库事务，失败时整体回滚并提示。

### 4.5 导出

- 用户选择新的 `.xlsx` 输出路径。
- 以应用保存的工作簿副本为基础生成导出文件。
- 在现有数据的最后一列新增表头 `Tags`，按源行映射写入 Tag 普通字符串。
- 多个 Tag 默认用 `, ` 连接。导出文件用于查看和交换，SQLite 仍是可编辑数据的唯一事实来源。
- 不覆盖已存在文件，除非用户在系统文件对话框中明确确认。
- 导出失败不得留下看似成功的半成品文件。

## 5. 图片策略

图片按以下顺序加载：

1. G 列路径存在时读取原图。
2. G 列路径失效时，从应用保存的 Excel 副本读取 A 列对应嵌入图。
3. 两者均不可用时显示明确占位状态。

性能要求：

- 不一次性读取或传输全部图片。
- 列表只加载可视区域附近的缩略图。
- 缩略图在应用数据目录缓存，并设置可清理、可重建的缓存策略。
- 大图按需读取，离开预览后释放前端对象 URL。
- 图片格式根据实际内容或扩展名识别，不写死为 PNG。

## 6. 技术方案

### 6.1 技术栈

- 桌面框架：Tauri v2。
- 后端：Rust + Tauri Commands。
- 前端：模块化 HTML / CSS / TypeScript，优先保持轻量，不采用单个超大 HTML 文件。
- 业务持久化：SQLite，Rust 侧使用成熟的 SQLite crate，并启用事务和迁移。
- Excel 单元格读取：优先使用只读解析库。
- 嵌入图片读取与 Excel 导出：直接处理 XLSX 的 OOXML ZIP 包，避免完整重写工作簿造成图片、样式或关系文件损坏。

不再采用 `umya-spreadsheet` 打开后整体保存原工作簿的方案。项目的关键要求是原文件不变、导出副本尽量保持原结构，因此导出时只替换目标工作表 XML 中新增 Tag 单元格所需的内容，其余 ZIP 条目应保持不变。

### 6.2 数据模型

建议的核心表：

```text
workbook
  id, imported_name, imported_at, sheet_name, row_count

rows
  id, source_row, time, positive_prompt, negative_prompt,
  artists, image_folder, image_path, embedded_image_ref

tags
  id, name COLLATE BINARY, created_at

row_tags
  row_id, tag_id

settings
  key, value
```

约束：

- `tags.name` 使用二进制排序规则和唯一索引，确保大小写敏感。
- `row_tags` 对 `(row_id, tag_id)` 建立唯一约束。
- `source_row` 保存原始 Excel 行号，用于导出回填。
- 数据库 schema 必须版本化，应用升级时通过迁移脚本升级。

### 6.3 后端职责

- 数据目录初始化和迁移。
- Excel 导入校验、复制和解析。
- 分页查询、Tag 聚合、AND / OR 筛选。
- 单行及批量 Tag 事务。
- 图片定位、缩略图生成和缓存。
- 基于工作簿副本的 OOXML 导出。
- 错误分类和用户可理解的错误信息。

### 6.4 前端性能

目标数据量为上万行，因此第一版必须包含：

- 后端分页或游标查询，不把全部行及长文本一次性发送到前端。
- 虚拟列表，只渲染可视区域附近的行。
- Tag 筛选和计数由数据库执行。
- 搜索或快速连续筛选使用防抖和请求取消/结果版本校验。
- 缩略图懒加载，并限制同时解码数量。
- 批量选择使用“筛选条件 + 排除项”的模型，避免全选上万行时在前端保存全部 ID。

## 7. 项目结构

```text
智能表格/
├── AGENTS.md
├── PLAN.md
├── PROGRESS.md
├── Examples/
│   └── novelai_metadata.xlsx
├── src/
│   ├── index.html
│   ├── styles/
│   └── scripts/
├── src-tauri/
│   ├── Cargo.toml
│   ├── tauri.conf.json
│   ├── capabilities/
│   └── src/
│       ├── commands/
│       ├── db/
│       ├── excel/
│       ├── images/
│       ├── lib.rs
│       └── main.rs
└── tests/
    └── fixtures/
```

具体目录可在脚手架阶段按 Tauri 官方结构微调，但业务模块边界保持独立。

## 8. 开发阶段

### M0 - 项目初始化

- 修订需求和技术计划。
- 建立 `PROGRESS.md`。
- 初始化 Git，配置 GitHub 远端并推送。

### M1 - 技术验证

- 验证样表只读解析。
- 验证嵌入图片与 Excel 行的映射。
- 制作最小 OOXML 导出原型，只在副本最后一列写入 Tag。
- 对比导出前后的 ZIP 条目，并用 Excel 实际打开验证图片、样式和文本未损坏。

M1 是后续开发的前置门槛，导出保真验证未通过前不搭建完整 UI。

### M2 - 应用数据层

- SQLite schema、迁移和事务。
- 首次数据目录设置。
- 单表导入、原样复制、失败回滚。
- 完整数据目录迁移和校验。

### M3 - 查询与 Tag 服务

- 分页查询和计数。
- 大小写敏感的自定义 Tag 库；Tag 定义与行关联分离，零关联 Tag 仍持久保留。
- AND / OR 筛选。
- 单行从 Tag 库复选并原子保存，批量从 Tag 库点选添加、删除。
- 全选筛选结果的数据模型。

### M4 - 主界面

- 表格式虚拟列表。
- 长文本折叠、展开、复制。
- Tag 筛选区和 AND / OR 切换。
- 行选择、当前页全选、筛选结果全选及批量操作。

### M5 - 图片系统

- G 列原图加载。
- 嵌入图回退。
- 缩略图缓存、懒加载和大图预览。

### M6 - 导出

- 文件保存对话框。
- OOXML 副本导出和 Tags 列写入。
- 临时文件、完整性校验和原子完成。

### M7 - 验收与发布

- 万行级测试数据验证。
- Windows 构建和安装包验证。
- 错误提示、空状态和迁移失败恢复。
- 发布首个 GitHub Release。

## 9. 验收标准

- [x] 原 Excel 在导入、编辑 Tag 和导出后内容及文件哈希均不变化。
- [x] 能正确导入固定结构表格，并对结构错误给出明确提示。
- [x] G 列图片存在时显示原图，失效时显示 A 列嵌入图。
- [x] Tag 区分大小写并能持久保存，未关联任何行的自定义 Tag 仍保留在 Tag 库。
- [x] AND / OR 全局筛选结果和计数正确。
- [x] 支持单行从已有 Tag 中复选，并支持勾选行及全部筛选结果从 Tag 库批量添加和删除。
- [x] 一万行数据下可正常分页、滚动、筛选，不一次性加载全部图片和长文本。
- [x] 重启应用后表格、Tag 和设置完整恢复。
- [x] 数据目录迁移成功后新目录可独立运行，失败时旧目录仍可用。
- [x] 导出的新 Excel 最后一列为 `Tags`，且嵌入图片、原数据和工作簿结构可正常使用。
- [x] 应用可在目标 Windows 环境安装和运行。

## 10. 开发约束

- 每完成一个可验证的小目标，更新 `PROGRESS.md`，提交并推送到 GitHub。
- 不把真实运行数据、数据库、缩略图缓存、构建产物和临时文件提交到仓库。
- 不更新本机 Node 版本；需要安装或升级其他环境组件时按项目环境规则执行。
- 临时脚本和一次性数据统一放在 `D:\Agent\Agent_temp`。
