# 智能表格 - NovelAI 图片资料库

## 0. 合并背景（2026-06-12 与用户对齐）

本项目由两个工具合并而成：

- **智能表格**（本仓库，宿主）：xlsx 导入 → SQLite 管理 Tag / 筛选 / 画廊 → 导出 xlsx。
- **Novelai工具**（`D:\Agent\Novelai工具`，并入后退役）：PNG 文件夹/压缩包 → 提取 NovelAI 元数据 → 输出 xlsx + 图片文件夹。

合并动机：xlsx 只是两个工具之间的中间件，本身不便管理（无 Tag、修改麻烦）。合并后外部内容直接导入资料库管理，xlsx 从"必经中间件"降级为"可选的导入/导出格式"。

已确认的合并决策：

1. **追加式资料库**：可多次导入，新行追加进现有库，Tag 永久保留（10000 行库导入 5 张图的压缩包 → 10005 行）。
2. **输出包功能转为可选导出**：imageN 图片文件夹、复制/硬链接从导入选项改为"按筛选/选中结果导出"。
3. **智绘姬 JSON 升级移植**：直接从库按筛选/选中结果导出 JSON，不再经过 xlsx；JSON 去重页原样移植。
4. **导入去重改为库内功能**：全部入库，应用内提供查重视图（按正向提示词/画师串分组），由用户决定删谁留谁。

## 1. 产品目标

1. 从 PNG 文件夹、`.zip`/`.rar`/`.7z` 压缩包、旧版 xlsx 导入 NovelAI 图片元数据，全部追加进同一个本地资料库。
2. 增量导入：已入库的图片自动跳过，导入结果报告"新增 X、跳过 Y"。
3. Tag 管理、AND / OR 筛选、画廊/表格双视图、详情面板（沿用现有能力）。
4. 库内查重视图与显式删除行。
5. 三种导出（均基于当前筛选/选中结果）：生成式 xlsx、智绘姬 JSON、图片文件夹输出包。
6. JSON 去重工具页。
7. 万行级数据下保持可用的加载、滚动和筛选性能（沿用现有要求）。

## 2. 产品规则（修订版）

- 资料库为追加式：导入不再替换工作区；删除行仅由用户在查重视图或详情中显式发起。
- 正常图片与压缩包/xlsx 原文件保持只读；文件夹/单 PNG 中元数据为空的异常图片按用户配置移动到异常图片目录。
- **增量跳过按"图片身份"判定**：
  - 文件夹/单 PNG：规范化绝对路径。
  - 压缩包图片：压缩包规范化绝对路径 + 包内相对路径。
  - xlsx 行：图片路径列（G 列）；该列为空时退化为 `xlsx路径 + 源行号`。
  - 身份相同即跳过，**不重复入库**；文件大小/修改时间有变化时也不自动更新已入库行（保护已打的 Tag），仅在导入结果中报告"已存在但内容有变化"的数量。
- 压缩包图片在导入时提取副本到受管数据目录，库引用副本；之后删除/移动压缩包不影响浏览。"图片路径"字段保留 `压缩包路径 > 包内路径` 形式的溯源信息。
- xlsx 导入时将 A 列嵌入图提取到受管数据目录（仅 G 列路径失效时作为显示回退），导入后不再依赖 xlsx 副本；"工作簿副本"机制退役。
- 文件夹/单 PNG/压缩包中读取失败，或解析后正向、负向提示词都为空的图片不入库；按原相对目录移动到用户配置的异常图片目录，重名自动编号，移动失败单独报告。
- Tag 由用户自定义、大小写敏感；筛选为全局 AND / OR；批量操作走数据库事务——全部沿用现有规则。
- 画师串只从正向提示词提取，规则沿用 Novelai工具（片段含 `artist` 即保留，换行连接）。
- 直接导入的图片"时间"取文件创建时间，格式 `YYYY-MM-DD HH:MM:SS`；取不到则留空。

## 3. 数据模型 v2

```text
import_batches                          -- 取代 workbook 单例表
  id, source_type ('xlsx'|'folder'|'archive'),
  source_path, imported_at, added_count, skipped_count

rows
  id INTEGER PRIMARY KEY,               -- 入库顺序即默认显示顺序
  batch_id REFERENCES import_batches ON DELETE RESTRICT,
  source_ordinal,                       -- 批内序号（xlsx 源行号 / 扫描序号）
  identity TEXT UNIQUE,                 -- 增量跳过的身份键
  source_size, source_mtime,            -- 变化检测（仅报告用）
  time, positive_prompt, negative_prompt, artists,
  image_folder,                         -- 旧 xlsx 列，直接导入时为空
  image_path,                           -- 原始路径 / "压缩包 > 包内路径" 溯源文本
  stored_image_path,                    -- 受管副本相对路径（压缩包图/嵌入图），可空
  metadata_failed INTEGER NOT NULL DEFAULT 0

tags / row_tags / settings              -- 不变（含 COLLATE BINARY 唯一索引）
```

要点：

- 行排序从 `source_row` 改为全局 `rows.id`（入库顺序）；`UNIQUE(workbook_id, source_row)` 退役。
- `identity` 唯一索引是增量跳过的依据；导入时用 `INSERT ... ON CONFLICT(identity) DO NOTHING` 类语义批量判定。
- `embedded_image_ref` 退役：v1→v2 迁移时把嵌入图从工作簿副本提取到受管 `files/`，行改为引用提取副本。
- **v1→v2 迁移**：旧 `workbook` 记录转为一个 `source_type='xlsx'` 批次；行按原 `source_row` 顺序重建保持显示顺序；Tag 与关联原样保留；迁移在事务内完成，嵌入图提取失败不丢行（仅失去嵌入图回退）。
- 删除行：级联清理 `row_tags`，同时删除该行的受管副本文件和缩略图缓存；不再被引用的 Tag 沿用现有清理规则。

受管数据目录新增 `files/` 子目录（按批次分目录存放提取副本），目录格式保持版本 1，`files/` 在打开时按需创建：

```text
<data-dir>/
├── smart-spreadsheet.sqlite3
├── files/<batch-id>/...          # 压缩包提取副本、xlsx 嵌入图（新增）
├── cache/thumbnails/
├── migration/
└── workbook/                     # 仅供 v1→v2 迁移读取，迁移后闲置
```

## 4. 导入管线（自 Novelai工具 移植）

移植模块：`png_text`（tEXt/zTXt/iTXt chunk 读取，Seek 跳过像素数据）、`metadata`（Description/Comment/v4 caption 解析）、`artist`（画师片段提取）、zip/7z/rar 解压（`zip`/`sevenz-rust`/`unrar-ng`，静态链接）、`parallel_map` 多线程基础设施（线程数跟随可用并行度，上限 32）。

**不移植**：`.novelai_metadata_cache` 增量缓存（SQLite 库本身承担此职责）、输出包/去重分组/时间前缀逻辑（输出包转为导出功能，去重转为库内功能）。

导入流程（folder / archive）：

1. 选择来源 → 压缩包解压到 `D:\Agent\Agent_temp` 运行临时目录；文件夹直接递归扫描 `.png`（跳过本工具旧版输出包目录与 `.novelai_metadata_cache`）。
2. 计算每张图的身份键，与库比对，已存在的跳过。
3. 新图并行读取元数据 + 生成缩略图；压缩包图复制副本到 `files/<batch-id>/`。
4. 单事务写入批次与行记录；失败整体回滚，不留半成品批次。
5. 进度事件节流（≥100ms）上报：扫描数、已处理、跳过、失败；完成后报告汇总。

xlsx 导入改造：沿用 calamine 固定结构解析与表头校验，语义从"替换"改为"追加 + 按身份跳过"；嵌入图在导入事务内提取到 `files/`。

前端：导入对话框（文件夹 / 压缩包 / xlsx 三入口）、进度条、结果 toast（新增/跳过/失败/变化计数）；顶栏提供"导入历史"简单列表（批次时间、来源、计数）。

## 5. 库内查重

- 后端：按"正向提示词"或"画师串"精确分组（裁剪首尾空白、区分大小写、空值不参与），返回组数与每组行摘要；万行级用 SQL 聚合完成，不在前端算。
- 前端：查重视图列出重复组（缩略图对比、提示词摘要、各行 Tag），勾选删除（保护机制：每组至少保留一行才能执行）。
- 删除走 M10 的删行能力，事务化、可多组批量执行，执行前明确确认。

## 6. 导出体系

三种导出均以"当前筛选结果或当前选中"为输入（沿用 explicit / filtered 选择模型）：

1. **xlsx**：用 `rust_xlsxwriter` 全新生成（移植 Novelai工具 的写表与缩略图嵌入逻辑），列结构沿用 7 列 + 末列 `Tags`；缩略图复用应用缓存。取代 v1 的 OOXML 补丁式导出（其"保持原工作簿不变"的前提在资料库模型下不存在了）。
2. **智绘姬 JSON**：正向提示词 → `fixedPrompt`、负向提示词 → `negativePrompt`、`fixedPrompt_end` 空串、顶层 `images` 空对象、`presets` 连续编号；逐条写入临时文件成功后原子替换（沿用 Novelai工具 实现）。
3. **图片文件夹输出包**：在所选位置新建 `智能表格图片导出[_N]` 输出目录，文件平铺命名 `00001_原名.png`（库内顺序前缀；实现时以扁平命名取代原 imageN 子目录设计——排序一致且更便于直接浏览），支持复制/硬链接（硬链接失败回退复制并汇总计数）。来源优先原路径、其次受管副本，缺图行计数返回。

通用规则：导出不覆盖已有文件（除非系统对话框确认）、临时文件 + 原子完成、失败不留半成品。

## 7. JSON 去重页

自 Novelai工具 原样移植：选择智绘姬 JSON → 校验 `presets` 结构、统计重复、预览前 3 条 → 按裁剪空白后的 `fixedPrompt` 保留首条 → 连续重编号输出新文件（默认 `_deduped` 后缀，临时文件原子替换）。作为独立工具页存在，不读写资料库。

## 8. 图片与缩略图策略（修订）

加载顺序：`image_path` 原路径 → `stored_image_path` 受管副本 → 占位状态。缩略图 256px / 预览 2048px、缓存键、懒加载、并发限制等全部沿用现有实现；删除行时清理对应缓存。

## 9. 技术栈

- 沿用：Tauri v2 + Rust + Svelte 5（runes）+ SQLite（rusqlite 0.39 bundled）+ calamine（xlsx 读取）。
- 自 Novelai工具 引入：`flate2`、`zip`、`sevenz-rust`、`unrar-ng`、`rust_xlsxwriter`、`walkdir`、`serde_json`（部分已有，以实际 Cargo.toml 为准）。
- 移除（M13 后）：OOXML 补丁导出路径相关代码（`excel/ooxml.rs` 中导出专用部分；嵌入图读取保留供导入用）。

## 10. 开发阶段

M0–M9 为合并前里程碑，已完成（见 `PROGRESS.md`）。

### M10 - 数据模型改造

- schema v2 迁移脚本（批次表、identity、删行、v1 数据迁移含嵌入图提取）。
- 行删除 API（级联清理副本与缩略图缓存）。
- 查询层排序改为 `rows.id`；分页、筛选、Tag 服务、选择模型适配（接口尽量不变）。

### M11 - 提取管线移植 + 导入

- 移植 PNG 解析、解压、并行处理模块及其测试。
- folder / archive 导入命令 + 身份跳过 + 副本提取 + 进度事件。
- xlsx 导入改追加语义 + 嵌入图提取。
- 前端导入流程与导入历史。

### M12 - 库内查重视图

- 重复分组查询 + 查重视图 + 批量删除（每组保底一行）。

### M13 - 导出体系 + JSON 去重页

- 生成式 xlsx 导出（替换 OOXML 导出）、智绘姬 JSON 导出、图片输出包导出。
- JSON 去重工具页移植。

### M14 - 验收与发布

- 万行级增量导入验收（10000 + 5 → 10005，重复导入不翻倍）。
- v1 旧数据目录升级验收（行序、Tag、嵌入图回退完整）。
- Windows 构建、GitHub Release；Novelai工具 仓库 README 标注已并入本项目。

候选后续迭代（不阻塞合并）：按时间排序的视图切换、批次筛选、库内变化文件刷新、缩略图 384px、深色主题。

## 11. 验收标准

- [x] 向已有库追加导入文件夹/压缩包/xlsx，新行追加且已有行与 Tag 不变。
- [x] 重复导入同一来源时全部跳过，行数不变；部分新图时只追加新图。
- [x] 压缩包导入后删除原压缩包，图片仍可浏览与预览（受管副本回退）。
- [x] 元数据失败的图片入库且带失败标记（详情展示；专门筛选入口列为候选迭代）。
- [x] v1 数据目录首次以 v2 打开后：行序、Tag、关联、嵌入图回退完整可用（v0.3.0 真实应用验收通过）。
- [x] 删除行后 Tag 关联、受管副本、缩略图缓存同步清理；被删行不再出现在任何查询。
- [x] 查重视图分组正确，删除时每组至少保留一行。
- [x] 三种导出基于当前筛选/选中结果，内容正确且不损坏，失败不留半成品（自动化覆盖；native 对话框链路建议实机抽查）。
- [x] 智绘姬 JSON 结构与 Novelai工具 输出一致；JSON 去重为同源移植（在智绘姬中实际加载建议实机抽查）。
- [x] 万行库导入、滚动、筛选、查重性能可用；原始输入文件在全流程中字节不变。

## 12. 第二轮迭代（v0.4，2026-06-12 与用户对齐）

v0.3 实机使用后用户反馈四项偏差，经逐项问答对齐如下。

### 12.1 导入按内容全库去重

- `rows` 新增 `content_hash` 列（schema v2→v3 迁移），对图片文件字节计算 SHA-256，建非唯一索引（历史行可能为 NULL，不能用 UNIQUE 约束）。
- 导入判定顺序：路径身份跳过（现有，第一层快速判定）→ 内容哈希跳过：本次导入内已出现同哈希、或库中任意行已有同哈希（无论来源路径/压缩包），均跳过不入库。导入含 500 张同内容图片的文件夹 → 只入库 1 行。
- 导入结果新增「内容重复跳过 N」计数，与按路径的「已入库跳过」分开报告。
- 旧库升级：迁移仅加列；打开数据目录时对缺哈希且字节可读（原路径或受管副本）的行补算哈希（带进度），不可读的行保持 NULL、不参与内容查重。

### 12.2 画廊/表格去重显示（取代库内查重浮层）

- 查询层新增去重维度参数：无 / 按正向提示词 / 按画师串（互斥）。SQL 端分组，每组取当前排序下的第一行作代表；键 trim 后为空的行不归组、照常全部显示。与 Tag 筛选叠加（先筛选后去重），影响画廊与表格两视图及「N 条匹配」计数。
- 前端在筛选区提供两个互斥勾选项。
- **退役**：「库内查重」浮层（DuplicatesView）、`find_duplicates` 命令、工具菜单对应入口（§5 方案由本节取代）。删除多余行改走 12.3 的通用删除。

### 12.3 通用删除功能

- 入口：选择栏「删除」按钮（作用于选中行）、详情面板单行删除、Delete 快捷键。
- 自定义确认弹窗（系统 confirm 不支持勾选项）：显示删除行数 + 勾选「同时将原始图片文件移入回收站」（默认不勾）。
- 勾选连原图时：文件夹/单 PNG 来源的原文件移入回收站（`trash` crate）；压缩包来源行没有独立原文件，跳过原文件删除并在结果汇总中说明；移入回收站失败的行计数报告、库行仍删除。
- 库行删除语义沿用现有 `delete_rows`：行 + Tag 关联 + 受管副本 + 缩略图缓存。

### 12.4 Tag 交互重做（方案 B：侧边栏双模式）

- 侧边栏顶部新增「筛选 | 打标」模式切换：
  - 筛选模式 = 现状（点击 Tag 切换筛选条件，AND/OR）。
  - 打标模式：面向当前选中行。每个 Tag 显示选中行覆盖状态（全有 / 部分 / 无），点击在「全部贴上 ↔ 全部移除」间切换（部分覆盖视为未全有，点击即全部贴上）；无选中行时显示提示。
  - 「即建即贴」输入框仅在打标模式显示：输入名字回车，不存在则新建 Tag 并立即贴到全部选中行。
- 详情面板单行 Tag 编辑保留，同样升级为即建即贴（输入不存在的名字回车 = 新建并贴上）。
- **退役**：选择栏的批量加/减 Tag 弹窗；侧边栏底部「新建 Tag」独立表单。

### 12.5 里程碑

- **M15 - 导入内容去重**：schema v3 迁移 + 打开时哈希补算 + 导入管线接入 + 计数上报。
- **M16 - 通用删除 + 去重显示**：删除入口/确认弹窗/回收站选项；查询去重参数与前端勾选项；查重浮层退役。
- **M17 - Tag 交互方案 B**：侧边栏双模式、即建即贴、批量弹窗与新建表单退役。
- **M18 - 验收与发布**：验收清单见 12.6，版本 0.4.0，Release v_4。

### 12.6 验收标准（v0.4）

- [x] 导入含 500 张同内容图片的文件夹 → 入库 1 行，其余计为「内容重复跳过」。
- [x] 库中已有图片 A（任意来源），从另一文件夹/压缩包再导入同内容文件 → 跳过、行数不变。
- [x] 旧库（v2）打开自动迁移 v3 并补算哈希，行/Tag 无损。
- [x] 勾选「按正向提示词去重 / 按画师串去重」后每组只显示一张代表图，空键行全部显示；与 Tag 筛选叠加正确。
- [x] 任意选中行可删除；勾选连原图时原文件进回收站、压缩包来源行跳过并在结果说明；不勾选时磁盘原文件不动。
- [x] 侧边栏打标模式点击 Tag 对全部选中行贴/解除；输入新名字回车即建即贴；筛选模式行为不变。

## 13. 第三轮迭代（v0.5，2026-06-12）

### 13.1 多选后的显式全选

- 选择栏在已有明确选择后显示「全选」按钮，点击后切换为 filtered 选择，覆盖当前 Tag 筛选与去重显示下的全部匹配行。
- `Ctrl+A` 快捷键继续保留；全选后选择栏显示筛选全选状态和排除数量。

### 13.2 空 metadata 图片隔离

- 文件夹/单 PNG/压缩包导入时，PNG 文本读取失败，或 NovelAI metadata 解析后正向、负向提示词均为空，均判定为异常图片。
- 异常图片不生成候选数据库行，不参与后续内容去重，也不保存为压缩包受管副本。
- 当前资料库保存一个用户自定义的异常图片输出目录；首次图片导入前必须选择，之后可从「工具」菜单更改。
- 异常图片按来源内相对路径移动到输出目录；同名目标自动追加编号，移动失败不回退入库，并在导入结果中报告成功与失败数量。

### 13.3 里程碑与验收标准

- **M19 - 显式全选**：选择栏入口 + 当前筛选全选行为。
- **M20 - 异常图片隔离**：目录设置 + 导入管线拒绝入库 + 移动与结果统计。
- **M21 - 验收**：Rust/前端全量验证与 Windows 构建。

- [x] 多选任意图片后点击「全选」，选中数量变为当前筛选/去重结果总数，批量操作作用域一致。
- [ ] 正常 metadata 图片照常入库；读取失败、空文本 chunk、仅含无提示词 Comment 的图片均不入库。
- [ ] 文件夹与单 PNG 异常图片移动到用户目录，保留相对路径；目标重名不覆盖已有文件。
- [ ] 压缩包异常图片从解压临时目录移动到用户目录，正常图片仍保存受管副本并可浏览。
- [ ] 异常图片移动失败时数据库仍不含该图，导入结果明确报告失败数量。

## 14. 开发约束

- 每完成一个可验证的小目标，更新 `PROGRESS.md`，提交并推送到 GitHub。
- 不把真实运行数据、数据库、缩略图缓存、构建产物和临时文件提交到仓库。
- 不更新本机 Node 版本；临时脚本和一次性数据统一放在 `D:\Agent\Agent_temp`。
- 移植 Novelai工具 代码时连同其测试一起移植；`D:\Agent\Novelai工具` 仓库只读参考，不在合并过程中修改。
