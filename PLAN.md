# 智能表格开发计划

## 产品定位

智能表格是本地图像资料库：直接从 PNG、文件夹或压缩包读取 NovelAI 元数据，
在 SQLite 中完成检索、Tag、分组、去重、备注和提示词维护。

电子表格不再参与导入或内部存储，只保留“把当前筛选/选中结果导出为 `.xlsx`”
这一项对外交换能力。

## 当前能力

1. 从单张 PNG、文件夹、`.zip`、`.7z`、`.rar` 追加导入。
2. 按规范化来源身份和 SHA-256 内容哈希跳过重复图片。
3. 解析 NovelAI PNG 文本块及 Alpha 通道 `stealth_pngcomp` 隐写元数据、
   V4 角色提示词、画师串和完整元数据指纹。
4. 支持 Tag、分组、搜索、筛选、相似图、重复聚合与批量提示词编辑；全文搜索兼容
   单行输入框把多行提示词的换行替换为空格或直接删除，确保从详情复制完整提示词仍可命中；
   批量 Tag 编辑走暂存式对话框（覆盖率三态、搜索/补全/新建合一、最近使用置顶、
   一次应用单条撤销），Tag 支持重命名。
5. “更新现有图片”按内容哈希或完整元数据指纹安全重关联搬家/重编码后的原图；
   多候选时拒绝自动猜测。
6. 导出当前结果：
   - 带缩略图的生成式 XLSX；
   - 智绘姬 JSON：导出前可选择“备注_序号”命名、把资料库画师串中缺失的片段
     追加到正向提示词末尾，并按补齐后的完整正向提示词去重；重复组优先保留有
     文字备注的记录，同等条件保留最早入库项，去重完成后连续编号；
   - 轮询脚本 JSON：顶部“导出”和底部“导出所选”均提供入口，但严格要求用户先
     选中图片；一张图片对应一个项目，同时原样保存正向、角色和负向提示词；项目名
     按备注、原图片文件名、稳定序号依次回退，可由 NovelAI 提示词轮询器的
     “批量填入”按写入位置读取；
   - 原图/重编码图片文件包。
7. 数据目录迁移、缩略图缓存、提示词文档和撤销能力；迁移时按“统计、复制文件、
   备份数据库、校验文件、校验数据库、切换目录”上报分阶段进度，文件阶段
   显示已处理容量和总体进度条。迁移清单只遍历一次，复制业务文件时同步计算
   SHA-256，随后只顺序读取目标文件校验；可重建的缩略图缓存不参与迁移，Tag、
   分组、备注、提示词等 SQLite 数据仍通过 Online Backup 完整保留。详情面板的
   备注与三类提示词编辑支持回车保存、
   Shift+回车换行，并保留 Esc 取消。
8. 画廊图片分级加载：256px 缩略图立即显示，停滚后仅为可见卡片升级
   1024px 高清层；详情使用缓存化 2048px 预览，全屏按需读取完整原图。
   主画廊、
   分组与重复项共用的派生缓存按内容签名命名，写入热路径不扫描整个缓存目录，
   淘汰检查只在进程首次写入和累计新增 64 MiB 后触发，避免大资料库退化为 O(N²)。
   三种视图的预览卡片在图片左下角最多并排显示两个独立 Tag 胶囊，更多 Tag
   直接隐藏且不显示数量摘要；胶囊使用六色固定色板循环取色，左侧 Tag 列表
   同步显示对应色块，图片区域不叠加 Tag 遮罩。
9. 独立工具箱窗口：承载随机画师串、以图搜图、智绘姬 JSON 去重、感知哈希
   维护、失败图片目录、数据管理和全库快速整理；点击以图搜图或快速整理预览结果
   可在主窗口精确定位画廊图片；以图搜图的参考图可通过文件选择器选取，也可直接
   拖入工具卡开始搜索；主窗口只保留资料库浏览、导入与导出主流程。
10. 快速整理按正向提示词、角色提示词、负向提示词、画师串与备注中的严格 AND
    组合扫描整个资料库；可批量添加一个或多个 Tag，或把命中图片统一移入指定分组，
    Tag 与分组均支持在工具内新建；批量分组可选择仅接收未分组图片，避免命中图片
    离开既有分组；“提示词操作”可输入单个画师名，在正向、角色和负向提示词中把
    严格匹配的裸画师 Tag 批量补为 `artist:` 前缀，同时保留 NovelAI
    权重格式（包括跨逗号、以 `::` 结束的数值权重区段），并跳过已带前缀或仅名称相似的
    Tag。条件匹配内置少量明确的泛用别名，
    当前将 `girl`、`1girl`、`1 girl` 视为同一项。上述动作均支持预览、精确撤回与重做。
11. Tag 库筛选可只显示包含 NovelAI VIBE 引用的图片，并与分页、全选、批量操作、
    重复项浏览和当前结果导出共用同一筛选结果集。重复项视图支持“按 VIBE”聚合：
    导入时对 `reference_image_multiple` 各项分别取 SHA-256、排序后整体哈希，
    得到与引用顺序无关的组合签名；相同签名的图片聚为一组，组名默认为短标识，
    可右键重命名别名。历史资料库的签名回填由前端启动后触发后台命令完成，
    带进度条提示，不阻塞界面；失败或中途关窗下次启动自动续跑。
12. 工具箱图片导出：可处理主窗口当前选区，也可点击选择本地文件夹，或把单张/
    多张图片、单个/多个文件夹混合拖入指定区域；文件夹递归扫描子目录，支持格式
    与现有图片工具一致，按完整路径追加去重并按文件名自然排序。目标文件夹必选；
    可保留原名、使用随机乱码或按“自定义前缀_序号”重命名；PNG 抹除元数据时
    重新编码副本，同时清除附加块和 NovelAI Alpha 最低位隐写载荷，其他格式通过
    重新编码清除元数据。导出位置、重命名开关与模式、自定义前缀、元数据开关保存
    到当前资料库的 `settings` 表并在下次打开工具时自动恢复；临时追加的来源图片不持久化。
13. 通用 UI 图标（包括窗口控制按钮）统一使用按需导入的 Lucide Svelte 组件；
    应用品牌图标等专用视觉资源继续单独维护。
14. 画廊与表格共用资料库排序状态，支持时间正序、时间倒序和最近更新；提示词、
    备注、Tag 与分组变更都会刷新图片更新时间。排序入口使用文字与下拉箭头按钮、带说明、
    选中态的自定义弹出菜单，并为键盘操作与减少动态效果偏好提供兼容。“最常用”排序暂不实现。
15. 工具箱“画师前缀修正”只使用资料库自身的明确证据：如果任一提示词中已有
    `artist:名称`，则扫描其它正向、角色和负向提示词中的同名裸 Tag，并可批量补全
    `artist:`。没有明确库内证据的名称不会成为候选；匹配保留 NovelAI 权重和括号语法，
    支持查看示例、逐项选择、撤回与重做，且全程不联网。同一工具也可处理用户粘贴的
    纯文本：沿用库内明确证据和严格 Tag 匹配规则输出补全结果，支持复制，但不写入资料库。
    导入设置可开启自动补全：
    自动规则执行后仅检查本次新增图片，并在导入进度和结果中给出非阻塞提示，不要求确认。
16. 独立“自动规则”工具可在首次导入前编写并持久化规则。规则按顺序运行，可分别选择
    新图片导入后或更新现有图片后触发，也可先预览再手动应用到已有资料库。条件采用
    “条件组之间 AND/OR ＋ 组内 AND/OR ＋ 整体成立/不成立”的两层组合，覆盖提示词、
    Tag、分组、画师、备注、文件名/路径/大小、导入类型、VIBE、元数据状态、尺寸/比例、
    横竖构图以及模型、采样器、噪声调度、种子、步数、Guidance 和 CFG Rescale。
    任务覆盖 Tag 增删、分组设置/清除、提示词追加/精确删除/替换、画师前缀修正、备注
    设置/自动编号/追加/清空和按图片停止后续规则。备注自动编号按“前缀 + 数字”
    从整库已有同前缀备注的最大编号后续编，已经符合该格式的备注不会改写。
    每条规则独立事务隔离，失败会报告但不回滚
    已完成的导入，也不阻断后续规则；重复导入跳过的旧图片不会再次执行导入规则。
    Tag 增删任务使用可搜索的已有 Tag 多选器，分组设置必须显式选择已有分组，避免手输
    错字或默认选错目标；旧规则引用的已删除目标保留并给出警告，用户确认后再更换。
    规则支持以普通 `.json` 文件导入和导出单条或全部已保存规则，也可直接粘贴纯 JSON
    或 AI 回复中的单个 `json` 代码块。文件使用格式标识与版本号，分组引用按名称传递
    而非数据库 ID；导入前预览规则、重名改名及待创建的 Tag/分组，确认后原子写入，
    且所有导入规则默认停用并要求人工复核。
    自动规则 AI 编写助手以独立 Markdown 作为可维护的提示词源，完整描述对话确认
    流程、全部条件/任务、可移植 JSON 格式和安全边界；工具箱提供一键复制入口，
    复制时动态注入最新已有 Tag/分组列表。模板明确将数据解释为 AI 绘画生成元数据，
    区分逗号分隔的图片提示词项与资料库 Tag，并约束提示词 `value` 使用字符串格式。
    Tag、提示词组合和画师名等列表输入统一接受半角逗号、全角逗号或换行分隔，顿号
    `、` 保留为名称内容。
17. 画廊、表格和分组成员卡片的右键菜单可按当前图片的完整画师串进入独立浏览：
    触发时清除其它搜索、Tag 和开关筛选，按首尾空白归一后的画师串精确匹配全库图片；
    顶部筛选标签显示当前画师串并可单独取消。全选当前结果、批量操作和导出继续使用
    同一个精确筛选条件，避免可见范围与实际处理范围不一致。
18. 分组与重复项视图的成员卡片共享画廊选区能力：复选框、Ctrl 加选、Shift 按当前
    展开卡片顺序连选、右键批量操作、缩略图文件拖出及底部批量操作条保持一致。
    Ctrl+A 与操作条“全选”必须补取折叠/未加载成员，并只固化当前结构化视图的真实
    图片集合；切换到不同集合或改变重复项聚合依据时清空选区，禁止操作不可见行。
19. 图片支持直接拖出到资源管理器或 NovelAI；从选中图片开始拖动时一次带出完整
    选区，从未选中图片开始拖动时只带出当前图片。所有拖出项均解析为可验证的完整
    原件，任何一项无法取得原件时明确报错，不以缩略图或预览缓存降级替代。
20. 工具箱提供独立“应用更新”入口，默认不在启动时联网；用户手动检查后从
    GitHub Release 的 `latest.json` 获取新版本、发布时间与更新说明。发现新版时
    必须由用户明确确认才下载，界面展示字节进度；签名校验通过后交给 Windows
    NSIS 被动安装并自动重启。后台任务或工具箱撤回/重做记录尚未处理时禁止安装，
    避免强制退出丢失会话状态。更新包使用仓库外私钥签名，公钥固定在应用配置中。
21. 主窗口提供 Discord 风格“过滤”总控：Tag、分组、画师、提示词、VIBE、备注、元数据、
    横竖构图、图片尺寸/比例以及模型、采样器、噪声调度、种子、步数、Guidance 和
    CFG Rescale 均可由用户选择是否参与过滤；未选择的类别保持不限，已选条件之间
    使用 AND。面板内修改先保留为草稿，只有“应用过滤”才刷新结果，“取消”不改变
    当前结果，“清除过滤”重置草稿。生效条件显示在顶部标签中并可逐项移除；分页、
    当前结果全选、批量 Tag/分组/删除、重复项聚合及展开成员、XLSX/JSON/图片导出
    共用同一结构化筛选对象。筛选为当前会话状态，不写入资料库。
22. 独立“图片对比”窗口：画廊、表格、分组与重复项卡片的右键菜单“对比”以该图为
    样本打开单例窗口（与工具箱同机制；已打开时聚焦并切换样本）。样本区展示
    2048 大图、画师串、VIBE 数、模型徽章与可折叠复制的三类提示词；下方五个分区
    分别列出相同画师串、画师串 × 模型分布、相同 VIBE 组合 · 提示词不同、
    相同提示词 · VIBE 组合不同、相同提示词 · 模型版本不同（按模型分组，含未知
    模型组），每区按导入时间倒序页内分页，样本自身排除。“提示词相同”统一指
    正向与角色提示词去掉 CR/LF、首尾空白并忽略大小写后一致，负向提示词不参与
    判定。点击分区图片进入“样本 vs 该图”并排对比：两张大图同屏、提示词按逗号
    拆项的差异高亮（仅样本有/相同/仅该图有）与生成参数对比表（差异值高亮），
    支持复制该图 Prompt、在主画廊定位该图、按画师串去主画廊查看全部。样本缺失
    维度（无画师串/无 VIBE）与空结果分区显示如实文案。

## 工具箱边界

- 工具箱是带独立任务栏入口的单实例 Tauri 顶层窗口，重复打开时恢复并聚焦既有窗口。
- 工具面板在窗口内按需挂载，切换工具不会丢失已经输入或计算的临时状态。
- 以图搜图只在对应面板激活时接收本地文件拖放；一次接收一张支持格式的图片，
  拖放区提供进入反馈，不支持的文件或多文件拖入会给出明确提示。
- 资料库无数据时，只开放不依赖资料库的文件处理工具。
- 快速整理的临时规则不入库，关闭工具箱后清空；匹配条件与执行动作分层，
  画师前缀修正会同步重算正向与角色提示词对应的画师串，但负向提示词中的画师
  不进入画师串；后续可继续增加提示词替换、追加和删除等动作。
- 自动规则与快速整理相互独立：自动规则保存到 SQLite 并在导入/更新流程按配置触发；
  新库不预置任何业务规则、角色名或 Tag。用户示例仅用于自动化测试，不进入迁移脚本、
  默认配置或正式界面。自动规则的提示词组合按完整 Tag 匹配，只忽略大小写与常见
  NovelAI 权重外壳，不继承快速整理的 `girl / 1girl / 1 girl` 别名。
- 自动画师前缀修正只扫描 SQLite 中已入库的正向、角色和负向提示词，不重新读取、
  覆盖或改写用户原始图片；匹配以完整 Tag 为单位，保留 NovelAI 权重/括号语法，
  已带前缀和仅名称相似的 Tag 均跳过。
- “导入时自动补全画师前缀”是按资料库持久化的开关，默认关闭；开启后只处理本次
  真正新增的行，不回头修改旧图片。导入期间持续提示该步骤已启用，完成结果报告
  扫描、修正图片与字段数量；检查失败不回滚已经成功导入的图片。
- 导出工具通过跨窗口事件读取主窗口实时选区，也可递归扫描用户直接选择或拖入的
  本地图片与文件夹；两类来源合并后按规范化完整路径去重。输出直接写入用户选择的文件夹，
  同名文件自动追加序号且不覆盖；PNG 元数据清除的正确性由文本块、全块枚举和
  Alpha 隐写自动化测试覆盖，导出时不再逐张复检；批量任务按逻辑核心数和本批
  最大图片的估算工作内存自适应并发，快速 PNG 编码后不得改写资料库受管文件或用户原图。
  导出位置与处理选项自动记忆，待导出选区和外部来源始终只保留在当前会话。
- 数据目录迁移或重置成功后，通过跨窗口事件要求主窗口重新读取应用状态。
- 分组管理属于分组视图的上下文操作，不作为通用工具收进工具箱。
- 应用更新不依赖资料库，仅在用户打开工具并点击“检查更新”时访问网络；更新源固定为
  GitHub 最新正式 Release 中的 `latest.json`。Release 标签继续使用 `v_N`，清单里的
  `version` 使用应用 SemVer。Windows 发布必须同时上传安装器、同名 `.sig` 和
  `latest.json`；第一版带更新器的安装包仍需手动覆盖安装，后续版本才能内置更新。

## 数据模型

数据库当前 schema 为 v16。新库直接创建当前结构，不再重放早期电子表格迁移链。

- `import_batches`
  - 来源仅为 `folder`、`archive` 和只读历史标识 `legacy`。
- `rows`
  - 保存来源身份、提示词、备注、画师、原图路径、受管原图路径；
  - 保存内容哈希、感知哈希、完整元数据指纹；
  - 保存 VIBE 引用数量与组合签名，供资料库筛选、卡片状态与“按 VIBE”聚合复用；
  - 保存最近更新时间，历史行按导入时间回填，用户可见编辑由数据库触发器维护；
  - 保存图片宽高及 NovelAI 模型、采样器、噪声调度、种子、步数、Guidance、
    CFG Rescale，供自动规则条件直接判断；历史行保持空值，更新现有图片时补齐；
  - `stored_image_is_original` 明确受管副本能否作为完整原件使用。
- `automation_rules`
  - 保存规则名称、说明、启用状态、执行顺序、导入/更新触发开关，以及条件组和任务列表；
  - 新库保持空表，不包含任何预置角色或 Tag 规则。
- `tags` / `row_tags`
- `groups`
- `settings`
- `dedupe_aliases`
兼容策略：

- 新库直接创建 v16。
- v8–v15 数据库自动升级到 v16；历史电子表格批次转为 `legacy`，数据与 Tag 保留，
  既有图片的 VIBE 数量与组合签名在首次打开时自动回填。
- v14 及更早版本遗留的外部画师词典缓存表在升级时删除。
- 历史版本快速修正画师前缀时可能把多个画师保存为逗号分隔；新版首次打开资料库
  会从正向与角色提示词自动重建为逐行画师串，单画师筛选同时防御尚未重建的旧格式。
- v1–v7 不再携带内置升级代码，打开时提供明确的旧版升级提示。
- 数据目录中既有的未知/历史文件不会被主动删除，但当前运行时不再依赖 `workbook/`。

## XLSX 导出约束

- 使用 `rust_xlsxwriter` 全新生成，不读取或修改任何旧工作簿。
- 固定工作表名 `NovelAI Metadata`。
- 列为：图片、时间、正向提示词、角色提示词、负向提示词、画师串、
  图片文件夹、图片路径、备注、Tags。
- 导出目标不得覆盖已有文件，先写临时文件再原子归位。
- 个别图片缺失时保留该行文字并计入失败数。
- 测试直接验证 OOXML ZIP 结构、工作表行数、文字内容、绘图锚点和缺图降级。

## 后续方向

1. 合并 `agent/minimal-ui` 回 main 并发布新 release，收集真实使用反馈。
2. 视觉收尾杂项：SizeSlider 完整自定义（track/moz 前缀）、空状态观感提升；
   动效实测若详情面板宽度滑入引起画廊明显抖动则回退该项。
3. 持续优化大资料库筛选、图片缓存淘汰和批量操作性能。
4. 改善原图搬家冲突的人工选择流程。
5. 补充备份/恢复与数据库健康检查入口。
6. 持续压缩历史兼容面，避免退役格式重新进入运行时主链。
