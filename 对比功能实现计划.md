# 对比功能实现计划

最后更新：2026-08-23（阶段 1～5 全部完成）

## 0. 背景与结论

- 上一版对比窗口（提交 `f14767b`，已随 `7216ca9` revert）因 bug 太多被整体回退。
  本次**全新重做**，不直接恢复旧提交；旧代码只作为结构参考
  （`git show f14767b` 可查看：窗口路由、五分区、并排视图、提示词 diff 工具函数）。
- 与用户讨论后确定的最终形态见下文，所有口径均已确认，不再有未决需求问题。

## 1. 需求定义（已与用户确认）

### 1.1 入口

- 画廊、表格、分组成员、重复项成员卡片的右键菜单新增“对比”项。
- 点击后以该图片为**样本**，打开独立的对比窗口。

### 1.2 窗口形态

- **独立 Tauri 窗口**（用户明确选择），单实例：再次对其它图片点“对比”时，
  复用既有窗口、切换样本并聚焦。
- 窗口内容自上而下：样本卡 + 四个分区**纵向排开滚动**。
- 每个分区先显示第一页，提供“加载更多”，分区头显示匹配总数；
  无匹配时显示如实的空态文案（不隐藏分区）。

### 1.3 四个分区及匹配口径

| # | 分区名 | 匹配条件（均排除样本自身） |
|---|--------|--------------------------|
| ① | 相同画师串 | 画师串**整串**精确相同（首尾空白归一，与现有“只看当前画师串”同一口径） |
| ② | 相同vibe × 不同提示词 | vibe 组合签名（`vibe_signature`）相同，且画风签名与样本**不同** |
| ③ | 相同提示词 × 不同vibe | 画风签名相同，且 vibe 组合与样本**不同**（含“无vibe”作为一种取值） |
| ④ | 相同画风 × 不同模型 | 画风签名相同，且模型版本与样本**不同**；结果按模型版本分组展示 |

**“画风签名”= 全页统一的“相同提示词”口径**（用户确认）：

- 只比较**正向提示词**；角色提示词、负向提示词一律不参与。
- 比较前做归一化：去掉各版本 NovelAI **官方质量词**、忽略大小写、
  归一空白与换行（详见 §3.1）。
- vibe 分区（②③）与模型分区（④）使用同一个签名，全页只有一种口径。

### 1.4 点击图片后的动作：并排对比视图

- 点击分区里的任意卡片，进入**并排对比视图**（同窗口内切换，可返回分区列表）：
  - 左样本、右目标，两张渐进加载的大图；
  - 正向提示词按逗号分词做三类差异展示：仅样本有 / 双方共有 / 仅目标有；
    官方质量词淡化显示；
  - 生成参数对照表（模型、采样器、步数、种子、Guidance、CFG Rescale、
    噪声调度、尺寸），不同的行高亮；
  - 操作：在主画廊中定位样本 / 目标（复用以图搜图的跨窗口定位机制）、返回分区列表。

### 1.5 明确不做（本期边界）

- 纯只读浏览功能：不写库、不进撤销栈、无关闭守卫。
- 不做“部分 vibe 重叠”“含任一相同画师”等模糊匹配（画师串与 vibe 都是整体精确相同）。
- 不做相似构图（pHash）、相同种子、参数扫描等其它维度分区（讨论中定为暂缓）。
- 并排视图暂不做“把目标设为新样本”“同分区上一张/下一张”，列入可选后续。
- 不随本功能发布 release（用户要求时再发）。

## 2. 边界情况约定

| 情形 | 行为 |
|------|------|
| 样本无画师串 | 分区①显示空态“这张图没有画师串” |
| 样本无 vibe（`vibe_reference_count` 为 0 或 NULL 且非空串标记） | 分区②显示空态；分区③仍启用，语义为“同提示词、但引用了 vibe 的图” |
| 样本正向提示词为空 / 元数据解析失败（画风签名为 NULL） | 分区③④显示空态“这张图没有可比较的提示词” |
| 目标行 `vibe_signature` 为空串（“已扫描但文件不可读”标记） | 视为未知，从②③的匹配与“不同”判定中一律**排除** |
| 目标行画风签名为 NULL | 在②的“不同提示词”判定中视为不同；在③④中不参与（无法确认相同） |
| ④中目标模型无法识别为已知版本档位 | 退化为 `generation_model` 原始字符串比较；NULL/空串归入“未知模型”组 |
| 样本行在打开期间被删除 | 查询返回明确错误，窗口显示“样本已不存在”而非崩溃或空白 |
| 主窗口切换数据目录 / 迁移 / 重置 | 对比窗口监听现有跨窗口刷新事件，直接自动关闭（最安全） |
| 窗口打开期间库内容变化 | 不自动刷新；样本卡顶部提供手动“刷新”按钮 |

## 3. 关键设计

### 3.1 画风签名（style signature）

**归一化算法**（Rust 实现，新模块 `src-tauri/src/pipeline/style_signature.rs`）：

1. 统一换行为 `\n`；按半角逗号 `,` 与换行切分为 tag 序列（全角逗号不作分隔符，
   与提示词字段现状一致）。
2. 每个 tag：trim 首尾空白，内部连续空白折叠为单个空格。
3. 剥离官方质量词：将 tag 去除 NovelAI 权重外壳（`{...}`、`[...]`、`N::...::`）后
   与质量词总表（§3.2，各版本取并集、大小写不敏感）比对，命中则丢弃整个 tag。
   注：刻意加权的质量词（如 `1.5::very aesthetic::`）也会被剥离，这是接受的取舍——
   两侧同样剥离，比较仍然公平。
4. 丢弃空 tag；其余 tag 转小写；**保持原有顺序**（不排序，顺序不同视为不同提示词）；
   以 `,` 重新拼接。
5. 结果为空字符串 → 签名为 NULL（不参与匹配）；否则取 SHA-256 十六进制作为签名。

**存储**：`rows.style_signature TEXT` 新列 + 部分索引（schema v17，见 §4）。
计算只依赖库内已有的 `positive_prompt` 文本，**不需要读图片文件**。

**版本机制**：`settings` 表存 `style_signature_version`（整数，随质量词表/算法修订递增）。
启动回填时若库内记录的版本落后，则全量重算；这样以后修正质量词表只需改常量表并加版本号。

**写路径一致性（上一版 bug 的教训，重中之重）**：
新增集中入口 `style_signature::refresh_rows(conn, &[row_id])`，
所有写 `positive_prompt` 的地方在同一事务内调用。经模块审计，写路径共 6 处，
逐一接入并配测试：

1. `storage/import_images.rs` —— 导入新图（直接在插入时算好）；
2. `db/image_updates.rs` —— 更新现有图片；
3. `db/prompt_edit.rs` —— 详情面板编辑 + 批量提示词编辑；
4. `db/quick_edit.rs` —— 快速整理的提示词操作 / 画师前缀；
5. `db/artist_auto_prefix.rs` —— 画师前缀修正（手动工具 + 导入自动补全）；
6. `db/automation_rules.rs` —— 自动规则的提示词追加/删除/替换任务。

另写一个测试辅助函数“全表重算并断言与存量签名一致”，在上述每类操作的
集成测试末尾调用，防回归。

### 3.2 官方质量词表

初稿如下，**全部标记待核实**，阶段 1 必须先核实修正再定稿：

- Anime V3：`best quality, amazing quality, very aesthetic, absurdres`
- Furry V3：`{best quality}, {amazing quality}`（带花括号形式，剥壳后比对可覆盖）
- V4 Full / V4 Curated、V4.5 Full / V4.5 Curated：各自的默认质量词组合（待核实）

核实手段：

1. 查 `Examples\Inspect - NovelAI.html` 官方解析页与官方文档；
2. 写一次性统计脚本（放 `D:\Agent\Agent_temp`，用后即弃）：对用户真实库
   按模型版本统计正向提示词的公共尾部 tag，实证各版本自动附加了什么；
3. 由于匹配用的是**各版本并集**，宁可多收不可漏收——多收的词两侧同剥，
   不破坏匹配公平性；漏收则跨版本匹配直接失效。

### 3.3 独立窗口架构（消灭上一版的竞态类 bug）

- 新 Rust 命令 `open_compare_window(row_id)`，完全套用 `open_toolbox_window` 的
  后端单实例模式：
  - 窗口不存在 → 以 `index.html?window=compare&row=<id>` 创建，
    **样本 id 直接放在 URL 里**，新窗口自己拉数据，从根上消除上一版
    “推送样本 vs 窗口就绪”的竞态；
  - 窗口已存在 → 后端向该窗口 emit `compare://set-sample`（载荷仅 row id），
    并恢复、聚焦窗口；此时窗口早已初始化完毕，无竞态。
- 新增 `src-tauri/capabilities/compare.json`，**必须包含 `core:window:allow-destroy`**
  （吸取 v_26 关闭按钮失效的教训）及窗口最小权限集。
- `src/main.ts` 按 `?window=compare` 路由到 `CompareWindow.svelte` 外壳
  （52px 标题栏 + WindowControls + 可滚动画布，沿用工具箱的“画纸”语言）。
- 缩略图/大图加载复用现有资产协议与 `progressive-images` / `image-loader`
  基础设施（工具箱窗口已有同样用法，无新通道）。

### 3.4 后端查询命令

新模块 `src-tauri/src/db/compare.rs`，命令注册进 `app/commands.rs`。
全部只读、走索引的短查询，正常短持锁即可（不需要 `with_cloned_database`）。
分区①②③统一 `offset/limit` 分页并返回总数；卡片复用画廊现有的行摘要 DTO。

1. `get_compare_sample(row_id)` —— 样本完整信息：三类提示词、画师串、
   生成参数、vibe 数量/签名状态、画风签名是否存在（供前端决定空态文案）。
2. `query_compare_same_artists(row_id, offset, limit)` —— 分区①；
   画师串归一比较**复用**“只看当前画师串”既有的归一函数，不另写一份。
3. `query_compare_same_vibe_diff_style(row_id, offset, limit)` —— 分区②。
4. `query_compare_same_style_diff_vibe(row_id, offset, limit)` —— 分区③。
5. `query_compare_same_style_all_models(row_id, cap)` —— 分区④：
   返回画风签名相同的**全部**行（含模型字段，上限 cap=500，超限时返回截断标记，
   前端如实提示“仅显示前 500 张”）。模型版本归类沿用前端现成的
   `src/lib/utils/model-version.ts` 映射，由**前端**分组并剔除与样本同档位的组，
   避免把整套映射逻辑重复移植进 Rust。
6. 排序：各分区一律时间倒序（与画廊默认一致），④组内同样时间倒序，
   组间按版本从新到旧。

### 3.5 前端结构

```
src/lib/windows/compare.ts            打开窗口的 invoke 封装（仿 toolbox.ts）
src/lib/api/compare.ts                查询命令封装
src/lib/stores/compare-store.svelte.ts 样本 + 各分区独立状态（items/total/loading/error/page）
                                       + 视图栈（分区列表 ⇄ 并排视图）
src/lib/utils/prompt-diff.ts          逗号分词三类差异（可参考旧版实现重写）
src/lib/views/compare/
  CompareWindow.svelte                窗口外壳、URL 解析、set-sample 事件、关库自动关闭
  SampleCard.svelte                   样本卡：渐进大图、关键信息、可折叠提示词+复制
  CompareSection.svelte               通用分区：标题、计数、网格、加载更多、空态
  ModelGroupSection.svelte            分区④：按模型版本分组渲染（复用 model-version.ts 徽章）
  CompareCard.svelte                  分区卡片（尽量复用 Thumbnail/CardTagSummary 等现有件）
  SideBySide.svelte                   并排视图：双大图、提示词 diff、参数表、定位/返回
```

- 右键入口：`views/shell/ContextMenu.svelte`（画廊/表格）与
  `views/shell/SectionContextMenu.svelte`（分组/重复项成员）各加一项“对比”。
- 主画廊定位：复用 `ImageSearchTool` 已有的“恢复主窗口 → 清筛选 → 定位图片”命令/事件，
  不新造通道。

## 4. 数据库变更（schema v16 → v17）

```sql
ALTER TABLE rows ADD COLUMN style_signature TEXT;
CREATE INDEX idx_rows_style_signature ON rows(style_signature)
    WHERE style_signature IS NOT NULL;
-- vibe_signature 已有部分索引；artists 查询沿用现状（现有功能同口径，量级已验证可接受）
```

- 新库直接建 v17；v8–v16 自动升级（沿既有迁移链模式）。
- **回填**：新命令 `backfill_style_signatures`，纯 SQL 读算写、每 500 行一批
  （沿用 vibe 回填的分批与断点习惯），不读图片文件，5 万行预计秒级；
  由前端启动流程在现有 VIBE/模型回填之后触发；失败下次启动续跑；
  同时负责 `style_signature_version` 落后时的全量重算。

## 5. 分阶段实施

每阶段 = 一个小目标：完成即跑该阶段验证、更新 PROGRESS.md、commit 并推送 GitHub。

### 阶段 1：画风签名地基（纯后端，无 UI）

- 质量词表核实定稿（§3.2 的三个手段）＋ `style_signature` 归一化/签名函数；
- schema v17 迁移＋六个写路径接入＋启动回填＋版本重算机制。
- 测试：归一化边界（权重外壳、全角逗号不切分、换行、大小写、空提示词、
  纯质量词提示词）、各版本质量词剥离、六个写路径各自的签名一致性断言、
  回填幂等与版本升级重算、v16→v17 迁移。

### 阶段 2：查询命令

- `db/compare.rs` 五个命令＋注册。
- 测试：每个分区的口径（排除样本、空串 vibe 标记排除、NULL 签名规则、
  “无vibe”作为取值参与③）、分页与总数、④的 cap 截断标记、样本被删除的错误路径。

### 阶段 3：窗口与分区页骨架

- `open_compare_window` ＋ capability ＋ 路由 ＋ 窗口外壳；
- 样本卡＋四分区（纵向、分页、空态、刷新按钮）＋两处右键入口＋
  切库自动关闭＋复用窗口切样本。
- 验证：`npm run check`、生产构建、Playwright 模拟 Tauri IPC 冒烟
  （右键入口、四分区渲染、加载更多、各空态文案、set-sample 切换、
  ④分组与徽章、窄窗口无横向溢出）。

### 阶段 4：并排对比视图

**状态：已完成。**

- 点击卡片进入/返回、双大图渐进加载、提示词 diff（质量词淡化）、
  参数差异表、主画廊定位。
- 验证：prompt-diff 单测（含权重语法、重复 tag、空提示词）；Playwright 冒烟
  （进入/返回、diff 三类渲染、参数高亮、定位事件发出）。

### 阶段 5：收尾

**状态：已完成。**

- 用户真实库人工核对：跨版本“相同画风”命中率（质量词表正确性的最终检验）、
  四分区结果抽查、窗口生命周期（重复打开/切样本/关库）。
- 更新 PLAN.md（当前能力+工具箱边界处补对比窗口条目）与 PROGRESS.md；
  全量验证基线（Rust 测试、Clippy `-D warnings`、前端检查、生产构建）。
- 不发布 release，等用户要求。

## 6. 风险与对策

| 风险 | 对策 |
|------|------|
| 质量词表不全 → 跨版本匹配漏配（上一版“分区总是空”的最可能根因之一） | 阶段 1 用真实库实证核对；并集剥离宁多勿漏；版本号机制保证日后修表可全量重算 |
| 某个提示词写路径漏接签名刷新 → 签名过期、匹配错乱 | 集中 helper + 六路径审计清单 + 每路径集成测试末尾的全表一致性断言 |
| vibe 空串“不可读”标记混入匹配 | 查询里显式排除，专项测试覆盖 |
| 独立窗口生命周期（复用竞态、关库后悬空） | 样本 id 进 URL；复用仅传 row id 事件；关库事件直接关窗；Playwright 覆盖 |
| 大分区（热门画师串上千张）拖慢窗口 | 分页 + 总数；④设 500 上限并如实提示截断 |
| 旧库生成参数缺失影响④分组 | 模型名已有启动回填（今日已上线）；仍缺失的归入“未知模型”组，如实展示 |

## 7. 可选后续（本期不做，留作扩展）

- 并排视图内“设为新样本”链式浏览、同分区上一张/下一张切换。
- 新分区：相同提示词 × 不同画师串（配合随机画师串工作流）、相似构图（pHash）、
  相同种子。
- 画师串 / vibe 的模糊匹配模式（含任一相同画师、部分 vibe 重叠）。
